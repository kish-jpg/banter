# Xcode / SPM build artifacts (regenerated by `xcodegen generate` in CI)
.build/
DerivedData/
*.xcuserstate
.DS_Store
xcuserdata/
Banter.xcodeproj/

# GSD research web-fetch cache (regeneratable, not source of truth)
.planning/research/.cache/
infra/supabase/
.env.local

# Supabase cloud deploy: functions/coaching is a sync target (deploy-cloud.sh),
# Backend/functions/coaching is the tracked source of truth
supabase/functions/coaching/
supabase/.temp/
