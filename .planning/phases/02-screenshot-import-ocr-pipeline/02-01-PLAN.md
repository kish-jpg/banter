---
phase: 02-screenshot-import-ocr-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BanterShared/Package.swift
  - project.yml
  - BanterShared/Sources/BanterShared/Import/RecognizedLine.swift
  - BanterShared/Sources/BanterShared/Import/OCRPipeline.swift
  - BanterShared/Tests/BanterSharedTests/Fixtures/imessage_two_column.png
  - BanterShared/Tests/BanterSharedTests/OCRPipelineTests.swift
autonomous: true
requirements: [CAPT-01]
must_haves:
  truths:
    - "Both deployment-target sources (Package.swift, project.yml) declare iOS 18, unlocking the modern Vision RecognizeTextRequest API."
    - "A fixture screenshot run through OCRPipeline in the CI simulator returns the fixture's known text lines — proving Vision-in-simulator works (the phase's riskiest MEDIUM-confidence assumption) on the very first CI run."
  artifacts:
    - BanterShared/Sources/BanterShared/Import/OCRPipeline.swift
    - BanterShared/Sources/BanterShared/Import/RecognizedLine.swift
    - BanterShared/Tests/BanterSharedTests/Fixtures/imessage_two_column.png
    - BanterShared/Tests/BanterSharedTests/OCRPipelineTests.swift
  key_links:
    - "OCRPipeline output type RecognizedLine (text + normalized bottom-left CGRect) is the contract BubbleAttributor (plan 02) consumes."
    - "iOS 18 target in project.yml + Package.swift must agree byte-for-byte, or xcodebuild/swift test disagree on availability."
---

<objective>
Bump the whole project to iOS 18 (locked decision) and land a Wave-0 canary that proves Apple's Vision OCR actually runs against a static fixture image inside the CI simulator — the single riskiest MEDIUM-confidence assumption of this phase — before any downstream OCR/attribution/UI work is built on top of it.

Purpose: Fail fast. If Vision does not recognize fixture text in the macos-26 simulator, we learn it here in plan 01 (one cheap test) rather than in the last plan after the whole pipeline is wired. Also unlocks the async/await `RecognizeTextRequest` API the rest of the phase uses.
Output: iOS 18 deployment target in both config files; `RecognizedLine` contract type; `OCRPipeline.recognize(in:)`; one fixture PNG; one canary test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-screenshot-import-ocr-pipeline/02-RESEARCH.md
@.planning/phases/02-screenshot-import-ocr-pipeline/02-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (downstream plans import these):
- `RecognizedLine` (struct in BanterShared): `text: String`, `boundingBox: CGRect` (normalized, origin bottom-left, 0...1).
- `OCRPipeline` (enum/struct in BanterShared) with `static func recognize(in cgImage: CGImage) async throws -> [RecognizedLine]`.
- Fixture: `BanterShared/Tests/BanterSharedTests/Fixtures/imessage_two_column.png` (synthetic rendered two-column chat).
- Test class: `OCRPipelineTests` (extended by plan 02).
- Deployment target iOS 18 in `BanterShared/Package.swift` (`.iOS(.v18)`) and `project.yml` (`deploymentTarget: "18.0"` for both targets).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump deployment target to iOS 18 in both config files</name>
  <files>BanterShared/Package.swift, project.yml</files>
  <read_first>
    - BanterShared/Package.swift (current `.iOS(.v17)` line)
    - project.yml (two `deploymentTarget: "17.0"` lines, one per target)
    - .planning/phases/02-screenshot-import-ocr-pipeline/02-RESEARCH.md § "Pitfall 1" (why iOS 18 is required for RecognizeTextRequest)
  </read_first>
  <action>
    Change `BanterShared/Package.swift` platforms from `.iOS(.v17)` to `.iOS(.v18)`. Change BOTH `deploymentTarget: "17.0"` entries in `project.yml` (BanterApp and BanterKeyboard targets) to `"18.0"`. This is the locked decision passed by the orchestrator under user delegation (bump to iOS 18 to unlock the modern Swift Vision `RecognizeTextRequest` API); log it in the SUMMARY as the phase's Vision-API-gating decision. Do not add any `@available` fallback path — iOS 18 is now the floor.
  </action>
  <verify>
    <automated>grep -c 'v18' BanterShared/Package.swift | grep -q '^1$' && grep -c '"18.0"' project.yml | grep -q '^2$' && ! grep -qE 'v17|"17.0"' BanterShared/Package.swift project.yml</automated>
  </verify>
  <acceptance_criteria>
    - `BanterShared/Package.swift` contains exactly one `.iOS(.v18)` and no `.iOS(.v17)`.
    - `project.yml` contains exactly two `"18.0"` deployment targets and zero `"17.0"`.
  </acceptance_criteria>
  <done>Both config files declare iOS 18; no iOS 17 reference remains in either.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OCRPipeline + RecognizedLine + Vision-in-simulator canary test</name>
  <files>BanterShared/Sources/BanterShared/Import/RecognizedLine.swift, BanterShared/Sources/BanterShared/Import/OCRPipeline.swift, BanterShared/Tests/BanterSharedTests/Fixtures/imessage_two_column.png, BanterShared/Tests/BanterSharedTests/OCRPipelineTests.swift</files>
  <read_first>
    - .planning/phases/02-screenshot-import-ocr-pipeline/02-RESEARCH.md § "Code Examples → Vision OCR with the new Swift API (iOS 18+)" and § "Pitfall 2" (boundingBox origin bottom-left)
    - .planning/phases/02-screenshot-import-ocr-pipeline/02-RESEARCH.md § "Environment Availability" (Vision-in-simulator claim, MEDIUM confidence — this test is its proof)
    - BanterShared/Sources/BanterShared/Models/ConversationMessage.swift (existing model style + public-init convention to match)
  </read_first>
  <behavior>
    - Test loads Fixtures/imessage_two_column.png via Bundle.module, converts to CGImage, calls OCRPipeline.recognize(in:).
    - Asserts at least one returned RecognizedLine.text contains a known fixture word (e.g. a literal string you render into the fixture, such as "hey" and "sounds good").
    - Asserts each RecognizedLine.boundingBox has origin/size within 0...1 (normalized-coordinate sanity).
  </behavior>
  <action>
    Create `RecognizedLine` (public struct: `text: String`, `boundingBox: CGRect`; public init) in RecognizedLine.swift. Create `OCRPipeline` with `static func recognize(in cgImage: CGImage) async throws -> [RecognizedLine]` using `RecognizeTextRequest` (iOS 18 async API) at `.accurate` recognition level with language correction on, mapping each observation's `topCandidates(1).first.string` + `observation.boundingBox` into a RecognizedLine. Do NOT sort or filter here — raw recognized lines only (ordering/attribution is plan 02's BubbleAttributor). Add the fixture PNG: a synthetic two-column chat screenshot rendering at least two short messages with known literal text on left and right sides. Register `Fixtures/` as a test resource in Package.swift's testTarget (`resources: [.copy("Fixtures")]`) if not already. Write `OCRPipelineTests.testRecognizesFixtureText` per the behavior block. This is the fail-fast canary: if Vision returns nothing in the simulator, CI goes red here in wave 1.
    Note the x-threshold and reading-order logic live in plan 02, not here — keep OCRPipeline a thin Vision wrapper.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter OCRPipelineTests</automated>
  </verify>
  <acceptance_criteria>
    - `OCRPipeline.recognize(in:)` exists with the exact async signature above.
    - `swift test --package-path BanterShared --filter OCRPipelineTests` passes IN CI (Vision recognizes the fixture's known words).
    - Package.swift testTarget declares the Fixtures resource so Bundle.module can load the PNG.
  </acceptance_criteria>
  <done>CI run is green with OCRPipelineTests passing — Vision-in-simulator is proven, not assumed. RecognizedLine is available for plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user photo → on-device OCR | The picked screenshot's pixels enter BanterShared for OCR; must stay on-device (CAPT-04, already structurally guarded). |
| CI artifact surface | Test fixtures/logs are uploaded to GitHub Actions; must contain only synthetic data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | OCRPipeline receiving image bytes | high | mitigate | OCR is pure on-device Vision; no network call in OCRPipeline. Existing NetworkBoundaryGuardTests (Phase 1) still asserts no image bytes in any network DTO — unchanged, not weakened by this plan. |
| T-02-02 | Information Disclosure | Fixture PNG committed to repo | low | accept | Fixture is synthetic rendered text ("hey"/"sounds good"), never real conversation data — safe to commit and to appear in CI logs. |
| T-02-SC | Tampering | package installs | low | accept | No new external packages this phase (Vision/PhotosUI are OS-bundled) — Package Legitimacy Gate not triggered per RESEARCH § Package Legitimacy Audit. |
</threat_model>

<verification>
- `swift test --package-path BanterShared` green in CI (canary passes).
- Both config files at iOS 18, no iOS 17 residue.
- First CI run after this plan is the Vision-in-simulator proof gate.
</verification>

<success_criteria>
- iOS 18 is the deployment floor in Package.swift and project.yml.
- OCRPipeline.recognize returns fixture text lines with normalized bottom-left bounding boxes, proven green in the CI simulator.
</success_criteria>

<output>
Create `.planning/phases/02-screenshot-import-ocr-pipeline/02-01-SUMMARY.md` when done.
</output>
