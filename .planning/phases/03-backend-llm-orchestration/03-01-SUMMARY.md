---
phase: 03-backend-llm-orchestration
plan: 01
subsystem: api
tags: [deno, taxonomy, psychology-gate, validation, gottman, attachment-theory, self-disclosure]

# Dependency graph
requires: []
provides:
  - "Backend/functions/coaching/taxonomy.json - versioned allowlist/banlist artifact (COAC-06)"
  - "Backend/functions/coaching/taxonomy.ts - typed loader, allowedTagNames(), containsBannedTerm()"
  - "Backend/functions/coaching/validate.ts - validateCoachingResponse() gate"
  - "Backend/deno.json - Deno project config with test task, local deno test loop"
affects: [03-02-prompt-and-llm-adapter, 03-03-edge-function-and-ci]

# Tech tracking
tech-stack:
  added: ["Deno 2.9.1 (winget install, local Windows test loop)", "jsr:@std/assert@1 (Deno test assertions)"]
  patterns: ["Taxonomy-as-gate: one JSON artifact read by both prompt assembly (later plans) and post-generation validation", "Provider-agnostic validator: validate.ts takes a minimal structural type, never a vendor-specific response type"]

key-files:
  created:
    - Backend/functions/coaching/taxonomy.json
    - Backend/functions/coaching/taxonomy.ts
    - Backend/functions/coaching/validate.ts
    - Backend/tests/taxonomy.test.ts
    - Backend/tests/validate.test.ts
    - Backend/deno.json
    - Backend/deno.lock
  modified: []

key-decisions:
  - "Used Deno's with { type: \"json\" } import attribute directly (no readTextFile fallback needed) - Deno 2.9.1, freshly installed via winget, supports it natively; RESEARCH.md's Assumption A1 resolved in favor of the modern syntax."
  - "deno.json test task fixed to use 'deno test .' (not 'deno test Backend/') since the task's cwd is Backend/ itself - the plan's literal task string would have double-nested to Backend/Backend and failed; the top-level 'deno test Backend/ --allow-env --allow-read' command from the plan's own <verification> section still works correctly run from repo root."
  - "nodeModulesDir: false replaced with nodeModulesDir: \"none\" - the boolean form is deprecated in Deno 2.x and printed a warning on every run; equivalent semantics, no npm resolution."
  - "Committed Backend/deno.lock (generated by first deno test run) - pins jsr:@std/assert@1.0.19 for reproducible resolution in the 03-03 CI job."

requirements-completed: [COAC-06]

coverage:
  - id: D1
    description: "Taxonomy artifact (taxonomy.json) with versioned, non-empty allowlist covering Gottman/attachment/Aron frameworks and a banlist covering negging/scarcity/alpha framing, on a tracked (non-gitignored) path"
    requirement: "COAC-06"
    verification:
      - kind: unit
        ref: "Backend/tests/taxonomy.test.ts (5 tests)"
        status: pass
      - kind: other
        ref: "git check-ignore Backend/functions/coaching/taxonomy.json (exit 1, confirms tracked)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Post-generation validator (validate.ts) mechanically rejects off-allowlist tags, banned terms, AI-tell punctuation (em dash/semicolon), and wrong reply counts"
    requirement: "COAC-06"
    verification:
      - kind: unit
        ref: "Backend/tests/validate.test.ts (7 tests, using real taxonomy.ts not a mock)"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-07-04
status: complete
---

# Phase 3 Plan 1: Psychology Taxonomy Artifact + Post-Generation Validator Summary

**Versioned taxonomy.json (Gottman/attachment/Aron allowlist + negging/scarcity/alpha banlist) with a typed Deno loader, plus validate.ts as the mechanical COAC-06 gate rejecting off-allowlist tags, banned terms, and AI-tell punctuation — 12 Deno tests passing locally.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-03T20:57:00Z
- **Completed:** 2026-07-03T21:09:27Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Literal versioned taxonomy artifact (`taxonomy.json`) on a tracked path, covering 6 evidence-based framework entries (Gottman Method x2, attachment theory, Aron self-disclosure, reciprocity norm, active listening/validation) and an 8-term banlist (negging, scarcity, alpha male, beta male, push-pull, last-minute resistance, NLP, neg)
- Typed loader (`taxonomy.ts`) exporting `TaxonomyEntry`/`Taxonomy` interfaces, `allowedTagNames()`, and `containsBannedTerm()` — module-scope JSON import via Deno's modern `with { type: "json" }` syntax
- Post-generation validator (`validate.ts`) enforcing reply count (exactly 3), tag-in-allowlist, banned-term absence, and no em dash/semicolon — provider-agnostic (structural type only, no Gemini import)
- First local Deno test loop for the project: `deno test Backend/ --allow-env --allow-read` runs on the developer's Windows machine, no Docker required
- 12/12 tests passing across both test files

## Task Commits

Each task was committed atomically:

1. **Task 1: Taxonomy artifact + typed loader** - `14d08ec` (feat)
2. **Task 2: Post-generation validator** - `5bd775c` (feat)

**Plan metadata:** (this commit)

## Files Created/Modified
- `Backend/functions/coaching/taxonomy.json` - versioned allowlist/banlist artifact (COAC-06)
- `Backend/functions/coaching/taxonomy.ts` - typed module-scope loader, allowedTagNames(), containsBannedTerm()
- `Backend/functions/coaching/validate.ts` - validateCoachingResponse() gate, provider-agnostic
- `Backend/tests/taxonomy.test.ts` - 5 Deno.test() cases
- `Backend/tests/validate.test.ts` - 7 Deno.test() cases (uses real taxonomy.ts, not a mock)
- `Backend/deno.json` - Deno project config, test task
- `Backend/deno.lock` - lockfile pinning jsr:@std/assert@1.0.19

## Decisions Made
- Deno's `with { type: "json" }` import attribute used directly — no `Deno.readTextFile` fallback was needed since Deno 2.9.1 (freshly winget-installed for this plan) supports the modern syntax natively. RESEARCH.md's Assumption A1 is resolved: no fallback required.
- `deno.json`'s `test` task string fixed to `deno test .` rather than `deno test Backend/` — the task executes with `Backend/` as its own cwd, so the plan's literal string would have resolved to the non-existent `Backend/Backend/`. The plan's top-level verification command (`deno test Backend/ --allow-env --allow-read`, run from repo root) is unaffected and still passes.
- `nodeModulesDir: false` replaced with `nodeModulesDir: "none"` — the boolean form is deprecated in Deno 2.x and emitted a warning on every invocation; functionally identical (no npm resolution).
- `Backend/deno.lock` committed (auto-generated on first test run) so CI (03-03) resolves the same `jsr:@std/assert` version deterministically.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed deno.json test task double-nesting Backend/Backend**
- **Found during:** Task 2 (verifying `deno task test` works as the plan's deno.json intends)
- **Issue:** The plan specified `"test": "deno test Backend/ --allow-env --allow-read"` inside `Backend/deno.json` — but a task's working directory is the directory containing the deno.json, so this resolved to `Backend/Backend/`, which doesn't exist, and the task errored.
- **Fix:** Changed the task string to `deno test . --allow-env --allow-read` (correct relative to `Backend/` as cwd). The plan's separately-specified top-level command (`deno test Backend/ --allow-env --allow-read` run from repo root, used in `<verify>` blocks and `<verification>`) was unaffected and still passes as-is.
- **Files modified:** Backend/deno.json
- **Verification:** `deno task test` (from inside Backend/) and `deno test Backend/ --allow-env --allow-read` (from repo root) both run clean, 12/12 passed.
- **Committed in:** 5bd775c (Task 2 commit)

**2. [Rule 1 - Bug] Fixed deprecated nodeModulesDir boolean value**
- **Found during:** Task 2 (same verification pass)
- **Issue:** `"nodeModulesDir": false` is deprecated in Deno 2.0+ and printed a deprecation warning on every `deno task`/`deno test` invocation.
- **Fix:** Changed to `"nodeModulesDir": "none"`, the current non-deprecated equivalent — still means no npm/node_modules resolution.
- **Files modified:** Backend/deno.json
- **Verification:** No warnings on subsequent `deno task test` runs.
- **Committed in:** 5bd775c (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - bugs in the deno.json task config, caught while proving the local test loop actually works as intended)
**Impact on plan:** Both fixes are small, mechanical corrections to make the plan's own deno.json spec actually runnable; no scope creep, no architectural change.

## Issues Encountered
- Deno was not installed on this Windows dev machine at plan start. Installed via `winget install DenoLand.Deno` (v2.9.1) per the plan's environment_facts instruction. PATH did not refresh within the current shell session, so all `deno` invocations in this session used the full winget install path directly; a fresh shell will have `deno` on PATH automatically (winget confirmed "Command line alias added: deno").

## User Setup Required
None - no external service configuration required. (Deno install was a one-time local dev-machine setup, already completed as part of this plan's execution.)

## Next Phase Readiness
- `allowedTagNames()` and `containsBannedTerm()` are ready for `promptAssembly.ts` (03-02) to inject the allowed-tag vocabulary into the Gemini system instruction.
- `validateCoachingResponse()` is ready for `index.ts` (03-03) to call as the post-generation gate before any response reaches a client.
- No blockers. `deno test Backend/ --allow-env --allow-read` is green locally; CI backend job arrives in 03-03 as planned (this plan's proof was local-only, per environment_facts).

## Self-Check: PASSED

All created files verified present on disk; both task commits (14d08ec, 5bd775c) verified present in git log.
