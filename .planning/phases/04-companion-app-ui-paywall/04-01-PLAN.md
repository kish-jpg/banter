---
phase: 04-companion-app-ui-paywall
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - project.yml
  - BanterShared/Sources/BanterShared/Models/TaxonomyEntry.swift
  - BanterApp/Resources/taxonomy.json
  - Backend/scripts/sync-taxonomy.sh
  - BanterShared/Tests/BanterSharedTests/TonePickerTests.swift
  - BanterShared/Tests/BanterSharedTests/TagExplainerTests.swift
  - BanterShared/Tests/BanterSharedTests/SentimentTimelineStoreTests.swift
  - BanterShared/Tests/BanterSharedTests/DailyCapTrackerTests.swift
  - BanterShared/Tests/BanterSharedTests/EntitlementManagerTests.swift
  - BanterUITests/OnboardingFlowTests.swift
  - BanterUITests/PermissionPrimingTests.swift
  - Banter.storekit
autonomous: false
requirements: [COAC-04]
tags: [ios, swiftui, revenuecat, storekit, taxonomy, wave-0]
user_setup:
  - service: revenuecat
    why: "RevenueCat SPM package add must be human-verified per Package Legitimacy Gate (04-RESEARCH.md Package Legitimacy Audit); RevenueCat public API key is safe-to-embed but the SDK add is a supply-chain surface (T-04-SC)."
    dashboard_config:
      - task: "Confirm RevenueCat/purchases-ios v5.x is the official org repo before adding"
        location: "https://github.com/RevenueCat/purchases-ios"

must_haves:
  truths:
    - "Every Wave-0 test file exists and fails for the right reason (missing production symbol), not a compile error unrelated to the tested behavior."
    - "The client can decode the same taxonomy entries the backend ships, offline, with no network call."
    - "RevenueCat SDK types are resolvable from BanterApp so EntitlementManager can compile in later plans."
  artifacts:
    - "BanterShared/Sources/BanterShared/Models/TaxonomyEntry.swift (Codable, 5 fields matching taxonomy.json)"
    - "BanterApp/Resources/taxonomy.json (byte-synced copy of Backend/functions/coaching/taxonomy.json)"
    - "Banter.storekit StoreKit configuration file with a weekly premium product + 14-day intro offer"
    - "7 Wave-0 test files (5 XCTest, 2 XCUITest)"
  key_links:
    - "TaxonomyEntry field names/order == Backend/functions/coaching/taxonomy.ts TaxonomyEntry interface (framework, technique, tagName, explanation, citation)"
    - "project.yml declares RevenueCat SPM package so BanterApp target resolves `import RevenueCat`"
---

<objective>
Wave-0 foundations for Phase 4: add the one new SPM dependency (RevenueCat), the one new shared model (TaxonomyEntry) with an offline-bundled synced taxonomy copy, a StoreKit configuration file for local purchase/trial testing, and the seven failing test scaffolds every later plan verifies against. No user-facing UI ships here — this plan removes the "MISSING" test references so downstream tasks have real `<automated>` gates.

Purpose: Satisfy the Nyquist Wave-0 gaps enumerated in 04-RESEARCH.md and 04-VALIDATION.md, and gate the RevenueCat supply-chain add behind a blocking human-verify checkpoint (T-04-SC) before any code depends on it.
Output: RevenueCat resolvable, TaxonomyEntry decodable offline, Banter.storekit present, 7 test files failing-red on missing production symbols.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-companion-app-ui-paywall/04-RESEARCH.md
@.planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md
@.planning/phases/04-companion-app-ui-paywall/04-VALIDATION.md
</context>

## Artifacts this phase produces (Plan 01)

New symbols/files created by this plan (exclude from drift verification):
- `TaxonomyEntry` (struct, BanterShared) — fields: `framework`, `technique`, `tagName`, `explanation`, `citation`
- `BanterApp/Resources/taxonomy.json` (bundled resource)
- `Backend/scripts/sync-taxonomy.sh` (drift guard)
- `Banter.storekit` (StoreKit config, product id `com.banter.premium.weekly`, entitlement id `premium`)
- RevenueCat SPM package entry in `project.yml`
- Test files: `TonePickerTests`, `TagExplainerTests`, `SentimentTimelineStoreTests`, `DailyCapTrackerTests`, `EntitlementManagerTests`, `OnboardingFlowTests`, `PermissionPrimingTests`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify RevenueCat package legitimacy before add (T-04-SC)</name>
  <what-built>Nothing yet — this is the pre-install legitimacy gate required by 04-RESEARCH.md's Package Legitimacy Audit for the one new third-party dependency.</what-built>
  <read_first>
    - .planning/phases/04-companion-app-ui-paywall/04-RESEARCH.md (Package Legitimacy Audit + Registry Safety sections)
  </read_first>
  <action>Pause for a human to verify RevenueCat/purchases-ios legitimacy at github.com/RevenueCat/purchases-ios before Task 2 resolves the SPM package. No code is written in this task; it exists solely to gate the supply-chain add (T-04-SC).</action>
  <how-to-verify>
    1. Open https://github.com/RevenueCat/purchases-ios in a browser.
    2. Confirm: official `RevenueCat` org, 5.x release tags present (research cites v5.80.2, 2026-07-02), 3000+ stars, active maintenance.
    3. Confirm the SPM URL to be added is exactly `https://github.com/RevenueCat/purchases-ios.git` with dependency rule "Up to Next Major Version" from 5.0.0.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the SPM add in Task 2, or paste any concern that changes the package/version.</resume-signal>
  <acceptance_criteria>
    - Human confirms the repo is the official RevenueCat org and the version pin (5.x, up-to-next-major) before any package resolution runs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add RevenueCat SPM package + TaxonomyEntry model + synced bundled taxonomy + StoreKit config</name>
  <files>project.yml, BanterShared/Sources/BanterShared/Models/TaxonomyEntry.swift, BanterApp/Resources/taxonomy.json, Backend/scripts/sync-taxonomy.sh, Banter.storekit</files>
  <read_first>
    - project.yml (current XcodeGen targets/packages — add, do not restructure)
    - Backend/functions/coaching/taxonomy.ts (TaxonomyEntry interface: framework, technique, tagName, explanation, citation — fields + order to mirror)
    - Backend/functions/coaching/taxonomy.json (the `allowed` array whose entries the client bundles)
    - Backend/scripts/sync-fixture.sh (existing drift-guard precedent to mirror for sync-taxonomy.sh)
    - BanterShared/Sources/BanterShared/Models/ReplySuggestion.swift (Codable struct style: public struct, public let fields, public init)
  </read_first>
  <action>
Add a `RevenueCat` entry to `project.yml`'s top-level `packages:` map with url `https://github.com/RevenueCat/purchases-ios.git` and `from: "5.80.2"`, and add `- package: RevenueCat` (product `RevenueCat`) to the BanterApp target's `dependencies:`. Add a `BanterApp/Resources` source path if not already covered so the bundled taxonomy.json is copied into the app bundle.

Create `TaxonomyEntry.swift` in BanterShared as a `public struct TaxonomyEntry: Codable, Equatable` with exactly these `public let` fields in this order to match the backend interface: `framework: String`, `technique: String`, `tagName: String`, `explanation: String`, `citation: String`, plus a public memberwise init. Follow ReplySuggestion.swift's declaration style.

Create `BanterApp/Resources/taxonomy.json` as a byte-identical copy of `Backend/functions/coaching/taxonomy.json` (the client bundles the synced copy per 04-RESEARCH.md Pattern 1 / Open Question 2 — the "bundle a synced copy" default, not a new endpoint).

Create `Backend/scripts/sync-taxonomy.sh` mirroring `sync-fixture.sh`'s diff-guard shape: it copies `Backend/functions/coaching/taxonomy.json` to `BanterApp/Resources/taxonomy.json` and `diff`s them, exiting non-zero on drift so CI can catch a stale bundled copy. Treat the backend file as authoritative source.

Create `Banter.storekit` StoreKit configuration file defining one auto-renewable subscription: product id `com.banter.premium.weekly`, a weekly subscription in a group (e.g. `banter_premium`), with a 14-day free-trial introductory offer, and an entitlement identifier `premium` mapped to it. This is the local-testing product surface referenced by 04-VALIDATION.md Wave 0; the real price is read at runtime from the store, never hardcoded in Swift.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter TaxonomyEntry 2>&1 | grep -qiE "passed|Executed|Compiling|error:.*TaxonomyEntry" || true; bash Backend/scripts/sync-taxonomy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `BanterShared/Sources/BanterShared/Models/TaxonomyEntry.swift` contains `public struct TaxonomyEntry: Codable` with fields `framework`, `technique`, `tagName`, `explanation`, `citation` in that order.
    - `bash Backend/scripts/sync-taxonomy.sh` exits 0 (bundled copy is byte-identical to backend taxonomy.json).
    - `project.yml` contains `url: https://github.com/RevenueCat/purchases-ios.git` and BanterApp lists `package: RevenueCat` under dependencies.
    - `Banter.storekit` exists and contains a product id `com.banter.premium.weekly` and an entitlement/reference `premium`.
  </acceptance_criteria>
  <done>RevenueCat is a declared SPM dependency, TaxonomyEntry decodes the bundled taxonomy offline, the drift guard passes, and the StoreKit config defines the weekly premium product with a 14-day trial and `premium` entitlement.</done>
</task>

<task type="auto">
  <name>Task 3: Create 7 failing Wave-0 test scaffolds (5 XCTest + 2 XCUITest)</name>
  <files>BanterShared/Tests/BanterSharedTests/TonePickerTests.swift, BanterShared/Tests/BanterSharedTests/TagExplainerTests.swift, BanterShared/Tests/BanterSharedTests/SentimentTimelineStoreTests.swift, BanterShared/Tests/BanterSharedTests/DailyCapTrackerTests.swift, BanterShared/Tests/BanterSharedTests/EntitlementManagerTests.swift, BanterUITests/OnboardingFlowTests.swift, BanterUITests/PermissionPrimingTests.swift</files>
  <read_first>
    - BanterShared/Tests/BanterSharedTests/AppGroupRoundTripTests.swift (round-trip XCTest style + AppGroupStore usage)
    - BanterShared/Tests/BanterSharedTests/NetworkBoundaryGuardTests.swift (structural #filePath source-scan negative-guard style)
    - BanterShared/Tests/BanterSharedTests/GeminiKeyBoundaryGuardTests.swift (forbidden-token scan style)
    - BanterUITests/ScreenshotArtifactTests.swift (XCUITest launch/capture helpers to reuse)
    - .planning/phases/04-companion-app-ui-paywall/04-RESEARCH.md (Phase Requirements -> Test Map: exact behaviors per test file)
  </read_first>
  <action>
Create each test file asserting the behavior from 04-RESEARCH.md's Requirements->Test Map. Where the production symbol under test does not exist yet (it lives in a later plan), write the test to reference the intended symbol and API so the file fails-red on the missing symbol — this is the intended Wave-0 red state, not a bug.

- `TonePickerTests.swift` (COAC-02): assert that selecting a `ReplyStyle` produces an `AnalyzeConversationRequest.tone` equal to that style (round-trip of the 4 cases `.playful/.sincere/.witty/.direct`).
- `TagExplainerTests.swift` (COAC-04): assert a lookup helper returns the correct `TaxonomyEntry` (framework + explanation + citation) for a given `psychologyTag` string matching `tagName`, and returns nil for an unknown tag. Load from the bundled taxonomy.json.
- `SentimentTimelineStoreTests.swift` (CALC-02, CALC-03): (a) round-trip test — appending N events for one `conversationId` reads back N ordered `SentimentEvent`s; (b) isolation test — events for conversation A never appear in a query for conversation B; (c) CALC-03 negative structural guard: source-scan `SentimentTimelineStore.swift` (via `#filePath` navigation, NetworkBoundaryGuardTests style) asserting no forbidden identity tokens (`matchName`, `matchId`, `matchIdentity`) appear — enforcing per-conversationId-only keying by construction.
- `DailyCapTrackerTests.swift` (MONE-01): assert a free-tier counter blocks after N calls in a day and that a date-scoped key resets on a new day; assert nothing in the tracker hides or gates psychology tags (tags-always-visible is a UI guarantee, so this test only proves the counter caps generation, not tag visibility).
- `EntitlementManagerTests.swift` (MONE-02, MONE-03): drive a mocked entitlement source (a protocol seam, NOT a live RevenueCat/network call) through three states — free, trial-active, premium — asserting `isPremium`/access reflects each, and that trial-expired reverts to free without clearing timeline data. Define the protocol seam here so the test compiles without a live SDK.
- `OnboardingFlowTests.swift` (ONBD-01): XCUITest launching with a fresh-install seed argument, asserting the tap path Welcome -> import -> suggestions reaches a suggestions screen with zero paywall/signup UI; reuse `ScreenshotArtifactTests` launch/capture helpers.
- `PermissionPrimingTests.swift` (ONBD-02): XCUITest asserting the Photos priming explainer heading "Let Banter read your screenshot" appears before the system Photos prompt is triggered.

Do NOT let any negative-grep literal (`matchName`, `matchId`, `matchIdentity`) appear as prose inside a production file's comments in later plans — keep those tokens confined to this test file's forbidden-list array.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared 2>&1 | grep -qiE "error:|failed|cannot find" && echo "RED-as-expected" || echo "check: expected red on missing symbols"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files under `BanterShared/Tests/BanterSharedTests/` and both files under `BanterUITests/` exist.
    - `SentimentTimelineStoreTests.swift` contains a forbidden-token array including `matchName` and a `#filePath`-based source scan of the store file.
    - `EntitlementManagerTests.swift` defines a protocol seam (no `import RevenueCat` required to compile the test) and exercises free/trial/premium/expired states.
    - `swift test --package-path BanterShared` fails referencing missing production symbols (TonePicker/TagExplainer/SentimentTimelineStore/DailyCapTracker/EntitlementManager), i.e. red-for-the-right-reason.
  </acceptance_criteria>
  <done>Seven Wave-0 test files exist and fail-red on the not-yet-built production symbols; the CALC-03 negative structural guard and the mocked-entitlement seam are in place so later plans have real automated gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/SPM install → app binary | Third-party RevenueCat SDK code enters the app supply chain |
| bundled taxonomy.json → client | Backend-authored content bundled into the app; drift risk, not a trust risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | RevenueCat SPM add | high | mitigate | Blocking human-verify checkpoint (Task 1) confirms official org + version pin before resolution; version pinned `from: 5.80.2` up-to-next-major |
| T-04-01-DRIFT | Tampering | bundled taxonomy.json | low | mitigate | `sync-taxonomy.sh` diff-guard fails CI if the bundled copy drifts from the backend-authoritative source |
</threat_model>

<verification>
- `swift test --package-path BanterShared` compiles TaxonomyEntry and fails only on later-plan symbols.
- `bash Backend/scripts/sync-taxonomy.sh` exits 0.
- `project.yml` resolves RevenueCat (confirmed at CI xcodegen/build time).
</verification>

<success_criteria>
RevenueCat is a declared, human-vetted dependency; TaxonomyEntry decodes the offline-bundled taxonomy; Banter.storekit defines the premium weekly product with a 14-day trial and `premium` entitlement; all 7 Wave-0 test scaffolds exist and fail-red on missing production symbols.
</success_criteria>

<output>
Create `.planning/phases/04-companion-app-ui-paywall/04-01-SUMMARY.md` when done
</output>
