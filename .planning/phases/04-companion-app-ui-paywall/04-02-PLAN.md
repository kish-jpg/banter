---
phase: 04-companion-app-ui-paywall
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - BanterApp/Coaching/CoachingClient.swift
  - BanterApp/Coaching/SuggestionCardView.swift
  - BanterApp/Coaching/TonePickerView.swift
  - BanterApp/Coaching/TagExplainerSheet.swift
  - BanterApp/Coaching/CoachingResultModel.swift
  - BanterShared/Tests/BanterSharedTests/TonePickerTests.swift
  - BanterShared/Tests/BanterSharedTests/TagExplainerTests.swift
autonomous: true
requirements: [COAC-02, COAC-04]
tags: [ios, swiftui, coaching, network, taxonomy]

must_haves:
  truths:
    - "A confirmed transcript produces 3 rendered suggestion cards, each showing reply text + its psychology tag, always visible (never paywalled)."
    - "Selecting a tone (playful/sincere/witty/direct) re-requests coaching with that tone set on the request."
    - "Tapping a tag expands an inline plain-English explanation + citation read from the bundled taxonomy, with no network call."
  artifacts:
    - "BanterApp/Coaching/CoachingClient.swift (URLSession POST to /functions/v1/coaching)"
    - "BanterApp/Coaching/SuggestionCardView.swift"
    - "BanterApp/Coaching/TonePickerView.swift"
    - "BanterApp/Coaching/TagExplainerSheet.swift"
    - "BanterApp/Coaching/CoachingResultModel.swift (@Observable result state)"
  key_links:
    - "CoachingClient encodes AnalyzeConversationRequest(tone:) and decodes CoachingResponseDTO — no new request/response model invented"
    - "TagExplainerSheet looks up TaxonomyEntry by tagName == psychologyTag from bundled taxonomy.json"
    - "TonePickerView data source is ReplyStyle (existing 4-case enum), not a parallel enum"
---

<objective>
The first user-visible vertical slice of Phase 4: a confirmed transcript becomes 3 tagged suggestion cards (COAC-04 tags always visible), steerable by a tone picker (COAC-02) and each tag expandable to a cited plain-English "why this works" from the bundled taxonomy (COAC-04). Introduces `CoachingClient` — the app's first outbound network call — speaking only the existing locked DTOs.

Purpose: Deliver the core coaching result surface every other Phase 4 slice builds on (onboarding demo terminal, timeline ingest, paywall gating all consume this).
Output: A working result screen from a confirmed transcript to 3 cards with tone control and inline tag explainers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-companion-app-ui-paywall/04-RESEARCH.md
@.planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md
@.planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 02)

New symbols/files created by this plan (exclude from drift verification):
- `CoachingClient` (struct/enum, async throws `send(_:)` -> CoachingResponseDTO)
- `SuggestionCardView`, `TonePickerView`, `TagExplainerSheet` (SwiftUI views)
- `CoachingResultModel` (@Observable: replies, selectedTone, expandedTagIndices, loading/error state)
- `taxonomyEntry(for:)` lookup helper (may live in TagExplainerSheet or a small TaxonomyLookup)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoachingClient — first outbound network call, DTO-only</name>
  <files>BanterApp/Coaching/CoachingClient.swift, BanterShared/Tests/BanterSharedTests/TonePickerTests.swift</files>
  <read_first>
    - BanterShared/Sources/BanterShared/NetworkDTOs.swift (AnalyzeConversationRequest, CoachingResponseDTO — the locked contract to encode/decode; do not modify)
    - BanterShared/Sources/BanterShared/Import/OCRPipeline.swift (async throws function-based pipeline style to mirror)
    - BanterShared/Tests/BanterSharedTests/GeminiKeyBoundaryGuardTests.swift (secret-scan style; CoachingClient must carry no secret)
    - Backend/functions/coaching/index.ts (route: POST only, path /functions/v1/coaching)
    - Backend/scripts/smoke-coaching.sh (endpoint URL shape: {base}/functions/v1/coaching)
  </read_first>
  <behavior>
    - Test 1 (COAC-02 round-trip in TonePickerTests): building the request for a given ReplyStyle yields AnalyzeConversationRequest whose `tone` equals that style, for all 4 cases.
    - Test 2: encoding AnalyzeConversationRequest then decoding the CoachingResponseDTO fixture (coaching-response.sample.json) round-trips to 3 replies + sentiment.
  </behavior>
  <action>
Create `CoachingClient` as a thin `URLSession`-based transport (prefer a struct with an async throws method `send(_ request: AnalyzeConversationRequest) async throws -> CoachingResponseDTO`, OCRPipeline's function-style spirit). It: JSON-encodes `AnalyzeConversationRequest`, POSTs to the coaching endpoint at `{baseURL}/functions/v1/coaching` (baseURL injectable via init/config, defaulting to a localhost dev value — do not hardcode a production secret or key), sets `Content-Type: application/json`, and JSON-decodes `CoachingResponseDTO`. Surface a typed error on non-2xx. Do NOT define any new request/response struct — reuse the locked DTOs from NetworkDTOs.swift. Carry no `GEMINI_API_KEY` or any secret literal (structural guard applies).

Fill in `TonePickerTests.swift`'s COAC-02 round-trip assertions against the request-building path so the tone field is proven set for all 4 ReplyStyle cases.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter TonePickerTests</automated>
  </verify>
  <acceptance_criteria>
    - `CoachingClient.swift` contains `AnalyzeConversationRequest` and `CoachingResponseDTO` usage and no new request/response struct definition.
    - `CoachingClient.swift` contains the substring `/functions/v1/coaching` and no secret/api-key literal.
    - `swift test --package-path BanterShared --filter TonePickerTests` exits 0.
  </acceptance_criteria>
  <done>CoachingClient POSTs the existing request DTO and decodes the existing response DTO with a typed error path; COAC-02 tone round-trip is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SuggestionCardView + TonePickerView + CoachingResultModel</name>
  <files>BanterApp/Coaching/SuggestionCardView.swift, BanterApp/Coaching/TonePickerView.swift, BanterApp/Coaching/CoachingResultModel.swift</files>
  <read_first>
    - BanterApp/Import/ConfirmTranscriptView.swift (attributionChip, messageBubble card styling, safeAreaInset, reduceMotionAwareAnimation, clearedToast)
    - BanterApp/Import/ImportFlowModel.swift (@Observable final class, private(set) state, method-only mutation)
    - BanterApp/DesignSystem/BanterTokens.swift (all tokens — reuse verbatim, no new tokens)
    - BanterShared/Sources/BanterShared/Models/ReplySuggestion.swift (ReplyStyle 4 cases; ReplySuggestion fields text/psychologyTag/style)
    - .planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md (Screen 4.3 layout, states, copy)
    - .planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md (SuggestionCardView / TonePickerView pattern assignments)
  </read_first>
  <behavior>
    - Model exposes replies:[ReplySuggestion], selectedTone:ReplyStyle, loading/error/dailyCapReached flags; changing tone calls CoachingClient with the new tone and swaps in a skeleton loading state.
    - Card renders reply text (body role, textPrimary) + a tag chip (label role, textSecondary on surface at rest — never accent at rest).
    - Tone picker: 4 segments from ReplyStyle; active segment gets surface pill + accent 2pt bottom border; inactive textSecondary, no fill.
  </behavior>
  <action>
Create `CoachingResultModel` as `@Observable final class` with `private(set)` state: `replies: [ReplySuggestion]`, `selectedTone: ReplyStyle`, `isLoading: Bool`, `errorMessage: String?`, `expandedTagIndices: Set<Int>`, and a `dailyCapReached: Bool` flag (set by Plan 05 later; default false here so onboarding/demo is ungated per RESEARCH Pitfall 4). Provide `@MainActor func selectTone(_:) async` that sets the tone, shows loading, calls `CoachingClient.send`, and replaces `replies`. Mutate only via methods.

Create `SuggestionCardView` rendering one `ReplySuggestion`: `surface` fill, `Banter.Radius.lg`, `Banter.Spacing.md` padding; reply text `body`/`textPrimary`; a tag chip below (adapt ConfirmTranscriptView.attributionChip — `label` role, ≥44pt tap target, color `isExpanded ? Banter.Colors.accent : Banter.Colors.textSecondary`, trailing chevron.down/up on expand); a Copy action (SF Symbol `doc.on.doc`, textSecondary, 44×44pt) that copies reply text to `UIPasteboard.general` and shows the "Copied" toast (reuse the clearedToast `.overlay(alignment:.bottom)` shape). Tags are ALWAYS rendered — no branch hides them for any tier.

Create `TonePickerView` bound to the model's `selectedTone`, 4 segments from `ReplyStyle` allCases-equivalent (playful/sincere/witty/direct), styled per UI-SPEC 4.3 (active = surface pill + accent 2pt bottom border; inactive = textSecondary). Selecting a segment calls `model.selectTone`. Expose VoiceOver label `"Tone: {name}, {selected/not selected}"`. Use the reduceMotionAwareAnimation helper for selection change.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter TonePickerTests</automated>
  </verify>
  <acceptance_criteria>
    - `SuggestionCardView.swift` renders `psychologyTag` unconditionally (no `isPremium`/`dailyCapReached` guard around the tag view).
    - `TonePickerView.swift` iterates the 4 `ReplyStyle` cases and calls `model.selectTone`.
    - `CoachingResultModel` is `@Observable final class` with `private(set)` state and `@MainActor` async tone method.
    - `swift test --package-path BanterShared --filter TonePickerTests` exits 0.
  </acceptance_criteria>
  <done>Three suggestion cards render with always-visible tags, a Copy-to-clipboard toast, and a 4-way tone picker that re-requests coaching on change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TagExplainerSheet — inline expand from bundled taxonomy (COAC-04)</name>
  <files>BanterApp/Coaching/TagExplainerSheet.swift, BanterShared/Tests/BanterSharedTests/TagExplainerTests.swift</files>
  <read_first>
    - BanterApp/Import/ConfirmTranscriptView.swift (@ViewBuilder editingIndex conditional inline-expand pattern; reduceMotionAwareAnimation)
    - BanterShared/Sources/BanterShared/Models/TaxonomyEntry.swift (from Plan 01: framework, technique, tagName, explanation, citation)
    - BanterApp/Resources/taxonomy.json (bundled synced taxonomy from Plan 01)
    - .planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md (Screen 4.3 Tag Explainer: inline expand, heading=framework, body=explanation, "Source: {citation}")
    - .planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md (TagExplainerSheet pattern assignment)
  </read_first>
  <behavior>
    - Test (TagExplainerTests, COAC-04): a lookup helper returns the TaxonomyEntry whose tagName == a given psychologyTag (correct framework/explanation/citation), and returns nil for an unknown tag.
    - Multiple cards may be expanded simultaneously (expandedTagIndices is a Set), no accordion-exclusivity.
  </behavior>
  <action>
Implement a taxonomy lookup helper (`func taxonomyEntry(for tag: String) -> TaxonomyEntry?`) that decodes the bundled `taxonomy.json` (the `allowed` array) into `[TaxonomyEntry]` once and returns the entry whose `tagName == tag`, else nil — NO network call, NO second LLM call. Build `TagExplainerSheet` as an inline disclosure (per UI-SPEC override: inline expand, not `.sheet()`): when a card's index is in `expandedTagIndices`, the card grows to show framework (`label` role bolded), explanation (`body`/`textPrimary`), and a citation line (`label`/`textSecondary`) prefixed "Source: ". Animate with the reduceMotionAwareAnimation helper. If a tag has no taxonomy entry, degrade gracefully (show the raw tag text, no crash). Fill `TagExplainerTests.swift` with the lookup assertions (hit + miss).
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter TagExplainerTests</automated>
  </verify>
  <acceptance_criteria>
    - Lookup helper returns the correct `TaxonomyEntry` for a known `tagName` and nil for an unknown tag (proven by TagExplainerTests).
    - `TagExplainerSheet.swift` reads from bundled taxonomy and contains no URLSession/network call.
    - Expanded content shows framework, explanation, and a "Source: " citation line.
    - `swift test --package-path BanterShared --filter TagExplainerTests` exits 0.
  </acceptance_criteria>
  <done>Tapping a tag inline-expands its framework + plain-English explanation + citation from the offline taxonomy, matching the backend allowlist, with no network round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → coaching API | Confirmed transcript (structured text only) crosses to backend; CAPT-04 boundary already enforced by DTO types |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-LEAK | Information Disclosure | CoachingClient | medium | mitigate | CoachingClient carries no secret literal; only structured DTO text crosses the boundary (reuses CAPT-04-guarded types); base URL injectable, not a hardcoded production credential |
| T-04-02-TAG | Tampering (spec) | SuggestionCardView | high | mitigate | Tags rendered unconditionally — no tier guard around the tag view; MONE-01 "tags always visible" enforced by construction (no `isPremium`/cap branch wraps the tag) |
</threat_model>

<verification>
- `swift test --package-path BanterShared --filter TonePickerTests` and `--filter TagExplainerTests` both green.
- Full CI (build BanterApp + XCUITest) green on wave merge.
</verification>

<success_criteria>
A confirmed transcript renders 3 tagged cards; tone picker re-requests coaching with the chosen tone; each tag inline-expands a cited explanation from the offline taxonomy; tags are never hidden behind any tier.
</success_criteria>

<output>
Create `.planning/phases/04-companion-app-ui-paywall/04-02-SUMMARY.md` when done
</output>
