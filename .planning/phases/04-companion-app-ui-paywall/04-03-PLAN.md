---
phase: 04-companion-app-ui-paywall
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - BanterApp/Onboarding/OnboardingFlowModel.swift
  - BanterApp/Onboarding/WelcomeView.swift
  - BanterApp/Onboarding/PermissionPrimingView.swift
  - BanterApp/Onboarding/ValueDemoCoordinatorView.swift
  - BanterApp/ContentView.swift
  - BanterUITests/OnboardingFlowTests.swift
  - BanterUITests/PermissionPrimingTests.swift
autonomous: true
requirements: [ONBD-01, ONBD-02]
tags: [ios, swiftui, onboarding, permissions, mvp-slice]

must_haves:
  truths:
    - "A fresh install reaches 3 tagged replies (screenshot -> OCR -> confirm -> coaching) with zero signup and zero paywall/entitlement/cap interaction."
    - "A Photos permission explainer appears immediately before the system Photos prompt at the moment of need."
    - "Declining the Photos priming routes to the paste-text fallback — no dead end."
  artifacts:
    - "BanterApp/Onboarding/WelcomeView.swift"
    - "BanterApp/Onboarding/PermissionPrimingView.swift (generic, reusable by Phase 5 keyboard priming)"
    - "BanterApp/Onboarding/OnboardingFlowModel.swift"
    - "BanterApp/Onboarding/ValueDemoCoordinatorView.swift"
  key_links:
    - "ValueDemoCoordinatorView wraps the existing ImportFlowModel state machine and, after .confirm, calls CoachingClient (from Plan 02) — never DailyCapTracker/EntitlementManager (RESEARCH Pitfall 4)"
    - "PermissionPrimingView is parameterized (icon/heading/body/onContinue/onSkip) so Phase 5 reuses it"
---

<objective>
The value-before-paywall onboarding slice (ONBD-01, ONBD-02): a fresh-install user lands on a welcome screen, gets a contextual Photos explainer at the moment of need, and runs the real screenshot -> confirm -> coaching loop to see 3 tagged replies — all before any signup or paywall exists. This wires Plan 02's coaching surface into a first-run flow and hard-guarantees the demo path touches no entitlement/cap code.

Purpose: Deliver the phase's first success criterion (core value before any paywall) as a complete vertical slice on a fresh install.
Output: Welcome + Photos priming + ValueDemo coordinator, wired as the app's first-run entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-companion-app-ui-paywall/04-RESEARCH.md
@.planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md
@.planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 03)

New symbols/files created by this plan (exclude from drift verification):
- `OnboardingFlowModel` (@Observable: state enum .welcome/.permissionPriming/.importFlow/.suggestionsShown)
- `WelcomeView`, `PermissionPrimingView` (generic), `ValueDemoCoordinatorView` (SwiftUI views)
- Launch-argument seeds for XCUITest (e.g. `--reset-onboarding-state`)

<tasks>

<task type="auto">
  <name>Task 1: OnboardingFlowModel + WelcomeView + generic PermissionPrimingView</name>
  <files>BanterApp/Onboarding/OnboardingFlowModel.swift, BanterApp/Onboarding/WelcomeView.swift, BanterApp/Onboarding/PermissionPrimingView.swift</files>
  <read_first>
    - BanterApp/Import/ImportFlowModel.swift (@Observable state machine + #if DEBUG CommandLine.arguments seed pattern)
    - BanterApp/Import/ImportEntryView.swift (ScrollView/VStack shell + primary CTA recipe)
    - BanterApp/Import/ParsingProgressView.swift (failureContent: icon + heading + body + two-button shape → generic priming layout)
    - BanterApp/DesignSystem/BanterTokens.swift (tokens, reuse verbatim)
    - .planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md (Screens 4.1, 4.2 layout + copy)
    - .planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md (Onboarding pattern assignments)
  </read_first>
  <action>
Create `OnboardingFlowModel` as `@Observable final class` with a `private(set) var state` enum: `.welcome`, `.permissionPriming(type: .photos)`, `.importFlow`, `.suggestionsShown`, mutated only via named methods (`start()`, `advanceToPermissionPriming()`, `continueToImport()`, `skipPriming()`). Include the `#if DEBUG` + `CommandLine.arguments` seed pattern from ImportFlowModel, adding a `--reset-onboarding-state` argument that forces `.welcome` and clears the `@AppStorage` "seen priming" flag so XCUITest gets a deterministic fresh-install state. Track "priming already seen" via a simple `@AppStorage` boolean, not a new BanterShared model.

Create `WelcomeView`: reuse ImportEntryView's `ScrollView { VStack(spacing: Banter.Spacing.md) }` + `.background(Banter.Colors.background.ignoresSafeArea())` shell. SF Symbol `bubble.left.and.bubble.right.fill` at 56pt (accent tint), heading (display) "Never freeze on a reply again", body (body/textSecondary, centered) "Import a screenshot, get 3 great replies instantly — with the psychology behind each one.", primary CTA (borderedProminent/accent, 52pt) "Try It Now" → routes to permission priming (or straight to import if priming already seen). No signup/login screen.

Create `PermissionPrimingView(icon:heading:body:onContinue:onSkip:)` as a GENERIC parameterized view (Phase 5 keyboard priming will reuse it — write generic from the start): icon 64pt `textSecondary` tint (not destructive), heading (`heading` role), body (`body`/`textSecondary`), primary CTA (`.borderedProminent`/accent, 52pt) calling `onContinue`, secondary text-button (`accent` text, 44pt) calling `onSkip`. For the Photos instance, heading "Let Banter read your screenshot", body "We only look at the one screenshot you choose. It never leaves your device — Banter reads it, then forgets it." (must stay technically true — CAPT-04), Continue → triggers PhotosPicker/permission, "Not Now" → paste-text fallback.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared 2>&1 | grep -qiE "Executed|passed" && echo "shared-green" || echo "note: onboarding UI proven in CI XCUITest"</automated>
  </verify>
  <acceptance_criteria>
    - `PermissionPrimingView.swift` declares a parameterized initializer accepting `icon`, `heading`, `body`, `onContinue`, `onSkip` (generic, not Photos-hardcoded).
    - `WelcomeView.swift` contains the exact copy "Never freeze on a reply again" and CTA "Try It Now".
    - `OnboardingFlowModel.swift` contains a `--reset-onboarding-state` launch argument under `#if DEBUG`.
    - Photos priming body contains "never leaves your device".
  </acceptance_criteria>
  <done>Welcome and a reusable generic permission-priming screen exist, with a deterministic fresh-install seed for CI.</done>
</task>

<task type="auto">
  <name>Task 2: ValueDemoCoordinatorView — wire the ungated demo loop to the coaching surface</name>
  <files>BanterApp/Onboarding/ValueDemoCoordinatorView.swift, BanterApp/ContentView.swift</files>
  <read_first>
    - BanterApp/ContentView.swift (Group { switch model.state } state-driven dispatcher; current root view)
    - BanterApp/Import/ImportFlowModel.swift (existing state machine + confirm() terminal; transcript exposure)
    - BanterApp/Coaching/CoachingResultModel.swift (from Plan 02: selectTone/replies)
    - BanterApp/Coaching/CoachingClient.swift (from Plan 02: send(_:))
    - BanterApp/Coaching/SuggestionCardView.swift (from Plan 02)
    - BanterApp/Coaching/TonePickerView.swift (from Plan 02)
    - .planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md (ValueDemoCoordinatorView pattern assignment; RESEARCH Pitfall 4 wiring guard)
  </read_first>
  <action>
Create `ValueDemoCoordinatorView` following ContentView's `Group { switch state { ... } }` state-driven dispatcher shape (do NOT introduce NavigationStack push flow). It wraps the existing `ImportFlowModel` state machine unchanged and adds one terminal step after `.confirm`: on confirm, call `CoachingClient.send` (directly, or via `CoachingResultModel.selectTone`/an initial load) using a client-minted `conversationId: UUID()`, then transition to showing `SuggestionCardView`s + `TonePickerView` (the Plan 02 surface). Update `ContentView` (or app root) so a fresh install enters the onboarding coordinator (Welcome → priming → this demo loop) rather than jumping straight to import.

HARD GUARD (RESEARCH Pitfall 4 / ONBD-01): the demo path from Welcome to suggestions must NOT reference `DailyCapTracker` or `EntitlementManager`. This file (and OnboardingFlowModel) must contain zero occurrences of the tokens `DailyCapTracker` and `EntitlementManager`. The first coaching call during onboarding is ungated — no cap increment, no entitlement fetch.
  </action>
  <verify>
    <automated>grep -Lq -e "DailyCapTracker" -e "EntitlementManager" BanterApp/Onboarding/ValueDemoCoordinatorView.swift BanterApp/Onboarding/OnboardingFlowModel.swift && echo "ungated-demo-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `ValueDemoCoordinatorView.swift` uses a `switch`-based state dispatcher (no `NavigationStack` push-based flow) and calls `CoachingClient`/`CoachingResultModel` after confirm.
    - Neither `ValueDemoCoordinatorView.swift` nor `OnboardingFlowModel.swift` contains the token `DailyCapTracker` or `EntitlementManager`.
    - App root routes a fresh install into the onboarding coordinator.
  </acceptance_criteria>
  <done>A fresh install runs Welcome → Photos priming → real import → confirm → coaching → 3 tagged cards, with zero entitlement/cap code on the path.</done>
</task>

<task type="auto">
  <name>Task 3: XCUITest ONBD-01/ONBD-02 flow assertions</name>
  <files>BanterUITests/OnboardingFlowTests.swift, BanterUITests/PermissionPrimingTests.swift</files>
  <read_first>
    - BanterUITests/ScreenshotArtifactTests.swift (launch/capture/waitForLaunchAnimationToSettle helpers to reuse)
    - BanterApp/Onboarding/OnboardingFlowModel.swift (--reset-onboarding-state seed argument)
    - .planning/phases/04-companion-app-ui-paywall/04-RESEARCH.md (Requirements->Test Map ONBD-01/ONBD-02 rows)
    - .planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md (Screen 4.1/4.2 copy for element matching)
  </read_first>
  <action>
Complete the two XCUITest files begun as Wave-0 scaffolds. `OnboardingFlowTests` (ONBD-01): launch with `--reset-onboarding-state` (+ a transcript seed argument like `--seed-sample-transcript` so the loop reaches suggestions without real OCR), tap "Try It Now" through the flow, assert a suggestions element appears AND assert no paywall/signup element (e.g. no "Start Free Trial" / "Unlock unlimited coaching" text) is present on the path. `PermissionPrimingTests` (ONBD-02): launch fresh, advance to the Photos step, assert the heading "Let Banter read your screenshot" is visible before any system Photos prompt is triggered. Reuse `waitForLaunchAnimationToSettle`/`capture` (extract to a shared XCUITest helper if not already shared, since this is the second consumer).
  </action>
  <verify>
    <automated>grep -q "Start Free Trial" BanterUITests/OnboardingFlowTests.swift && grep -q "Let Banter read your screenshot" BanterUITests/PermissionPrimingTests.swift && echo "assertions-present"</automated>
  </verify>
  <acceptance_criteria>
    - `OnboardingFlowTests.swift` asserts a suggestions element exists AND asserts absence of a paywall element ("Start Free Trial" / "Unlock unlimited coaching") on the fresh-install path.
    - `PermissionPrimingTests.swift` asserts "Let Banter read your screenshot" precedes the system Photos prompt.
    - Both reuse the shared launch/capture XCUITest helpers.
  </acceptance_criteria>
  <done>CI proves a fresh install reaches suggestions with no paywall, and that the Photos explainer precedes the system prompt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding demo path → coaching API | First-run ungated coaching call; must not touch entitlement/cap state |
| app → Photos | Permission requested at moment of need behind an explainer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-GATE | Elevation of Privilege (spec) | ValueDemoCoordinatorView | high | mitigate | Structural grep guard: demo path files contain no `DailyCapTracker`/`EntitlementManager` token; first-run coaching call is ungated (ONBD-01) |
| T-04-03-PERM | Information Disclosure | PermissionPrimingView | medium | mitigate | Explainer shown at moment of need; copy states data never leaves device (CAPT-04-consistent); decline routes to paste fallback, no coercion |
</threat_model>

<verification>
- `swift test --package-path BanterShared` green.
- CI XCUITest: OnboardingFlowTests + PermissionPrimingTests green; no-paywall assertion passes.
- Grep guard: no entitlement/cap tokens on the demo path.
</verification>

<success_criteria>
A fresh install experiences the full core value (screenshot → tagged replies) before any signup or paywall; the Photos explainer appears at the moment of need; declining routes to paste fallback.
</success_criteria>

<output>
Create `.planning/phases/04-companion-app-ui-paywall/04-03-SUMMARY.md` when done
</output>
