---
phase: 04-companion-app-ui-paywall
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - BanterApp/Calculator/SentimentTimelineStore.swift
  - BanterApp/Calculator/ConversationHealthView.swift
  - BanterShared/Tests/BanterSharedTests/SentimentTimelineStoreTests.swift
autonomous: true
requirements: [CALC-02, CALC-03]
tags: [ios, swiftui, charts, sentiment, privacy, persistence]

must_haves:
  truths:
    - "Each coaching response appends one SentimentEvent to a per-conversationId on-device timeline; N responses produce N ordered events."
    - "A conversation shows a health score + a Charts timeline + 4 factor mini-cards, scoped to that conversationId only."
    - "No store method or view accepts a match name/identity — timeline access is keyed exclusively by conversationId (CALC-03)."
  artifacts:
    - "BanterApp/Calculator/SentimentTimelineStore.swift (AppGroupStore-backed, per-conversationId)"
    - "BanterApp/Calculator/ConversationHealthView.swift (score + Charts timeline + factor grid)"
  key_links:
    - "SentimentTimelineStore keys strictly on 'timeline.\\(conversationId)' via existing AppGroupStore read/write"
    - "ConversationHealthView renders SentimentFactors (interest/reciprocity/warmth/responsiveness) and the mandatory CALC-03 scope caption"
---

<objective>
The love-calculator vertical slice (CALC-02, CALC-03): every coaching response feeds an on-device, per-conversation sentiment timeline, rendered as a health score + Charts line/area chart + 4 emotional-factor mini-cards — scoped strictly to one conversationId, never a cross-conversation match dossier. Persistence is client-side only (Phase 3 is stateless); the privacy boundary is enforced by construction and by a negative structural test.

Purpose: Deliver the phase's per-conversation health-timeline success criterion while structurally guaranteeing the CALC-03 no-dossier boundary.
Output: SentimentTimelineStore + ConversationHealthView, wired to ingest CoachingResponseDTO.sentiment.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-companion-app-ui-paywall/04-RESEARCH.md
@.planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md
@.planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 04)

New symbols/files created by this plan (exclude from drift verification):
- `SentimentTimelineStore` (struct/enum: `append(from:conversationId:messageIndex:speaker:)`, `events(for conversationId:)`)
- `ConversationHealthView` (SwiftUI view: health score, Charts timeline, factor grid, empty/single-point states)
- Optional `Banter.Colors.chartPositive` / `chartNegative` + `ChartPositiveColor`/`ChartNegativeColor` asset sets (only if UI-SPEC chart-color addition is taken; fallback is a neutral line)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SentimentTimelineStore — per-conversationId persistence + CALC-03 guard</name>
  <files>BanterApp/Calculator/SentimentTimelineStore.swift, BanterShared/Tests/BanterSharedTests/SentimentTimelineStoreTests.swift</files>
  <read_first>
    - BanterShared/Sources/BanterShared/AppGroupStore.swift (generic read/write Codable primitive; suiteName)
    - BanterShared/Sources/BanterShared/Models/SentimentEvent.swift (existing 6-field model: conversationId, messageIndex, speaker, scoreDelta, signal, timestamp)
    - BanterShared/Sources/BanterShared/NetworkDTOs.swift (CoachingResponseDTO.sentiment: SentimentDTO score/factors/signal)
    - BanterShared/Tests/BanterSharedTests/NetworkBoundaryGuardTests.swift (#filePath forbidden-token structural-guard style for the CALC-03 negative test)
    - BanterShared/Tests/BanterSharedTests/AppGroupRoundTripTests.swift (round-trip test style)
    - .planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md (SentimentTimelineStore pattern assignment + Pitfall 1)
  </read_first>
  <behavior>
    - Round-trip: appending N events for one conversationId reads back N events in append order.
    - Isolation: querying conversation B never returns conversation A's events.
    - CALC-03 negative guard: source-scan of SentimentTimelineStore.swift finds none of the forbidden identity tokens.
  </behavior>
  <action>
Create `SentimentTimelineStore` as a thin wrapper over `AppGroupStore`, keyed strictly by `"timeline.\(conversationId)"`. Provide `func append(from response: CoachingResponseDTO, conversationId: UUID, messageIndex: Int, speaker: Speaker)` that builds one `SentimentEvent` (scoreDelta = `response.sentiment.score`, signal = `response.sentiment.signal`, timestamp = Date()) and appends it to the read-modify-write array; and `func events(for conversationId: UUID) -> [SentimentEvent]`. Cap the stored array at the last 200 events per conversation (Pitfall 1) — drop oldest beyond the cap.

EVERY method signature must accept only `conversationId: UUID` — never a match name/identity string. Do not add any parameter named for a match identity. Keep the store free of the tokens the CALC-03 negative test forbids.

Complete `SentimentTimelineStoreTests.swift` (from Plan 01 scaffold): round-trip (N events), isolation (A vs B), and the CALC-03 structural negative guard (forbidden-token source scan of SentimentTimelineStore.swift, NetworkBoundaryGuardTests style).
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter SentimentTimelineStoreTests</automated>
  </verify>
  <acceptance_criteria>
    - `SentimentTimelineStore.swift` methods accept only `conversationId: UUID` (no match-identity parameter) and key on `"timeline.\(conversationId)"`.
    - Round-trip and A/B isolation tests pass.
    - The CALC-03 negative structural test passes (no forbidden identity token in the store source).
    - `swift test --package-path BanterShared --filter SentimentTimelineStoreTests` exits 0.
  </acceptance_criteria>
  <done>Sentiment events persist on-device per conversation, capped at 200, provably isolated per conversationId with a structural no-dossier guard.</done>
</task>

<task type="auto">
  <name>Task 2: ConversationHealthView — score + Charts timeline + factor grid</name>
  <files>BanterApp/Calculator/ConversationHealthView.swift</files>
  <read_first>
    - BanterApp/Calculator/SentimentTimelineStore.swift (from Task 1: events(for:))
    - BanterApp/Import/ConfirmTranscriptView.swift (pinned header shape + centered empty-state shape)
    - BanterShared/Sources/BanterShared/NetworkDTOs.swift (SentimentFactors: interest/reciprocity/warmth/responsiveness)
    - BanterApp/DesignSystem/BanterTokens.swift (tokens; chart-color addition rules)
    - .planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md (Screen 4.4 layout, states, mandatory scope caption, accessibility)
    - .planning/phases/04-companion-app-ui-paywall/04-PATTERNS.md (ConversationHealthView pattern assignment; Charts guidance)
  </read_first>
  <action>
Create `ConversationHealthView` taking a `conversationId: UUID` and reading `SentimentTimelineStore.events(for:)`. Layout per UI-SPEC 4.4:
1. Pinned header (`heading` role) "Conversation Health" (mirror ConfirmTranscriptView header shape).
2. Health-score card: `surface` fill, `Banter.Radius.lg`, large numeral (`display` role) for an aggregate score derived from the events' scoreDelta series (choose a simple bounded aggregation, e.g. normalized cumulative score 0–100), caption (`label`) "Health Score".
3. MANDATORY permanent caption (`label`/`textSecondary`) "Scoped to this conversation only — never shared across matches." — this is the user-facing CALC-03 guarantee, not optional fine print.
4. `Charts` line/area chart (`import Charts`): X = messageIndex/timestamp within THIS conversation only, Y = score. Use `LineMark`/`PointMark`/`AreaMark` (no custom Canvas). Line color: use `chartPositive`/`chartNegative` if the UI-SPEC color addition is taken (add two `Banter.Colors` entries + matching `ChartPositiveColor`/`ChartNegativeColor` asset sets), OR the safe fallback of a neutral `textPrimary` line with positive/negative background tint — either satisfies the spec.
5. 2×2 factor grid from the latest event's factors (`interest`/`reciprocity`/`warmth`/`responsiveness` → title-cased "Interest"/"Reciprocity"/"Warmth"/"Responsiveness"), each a small `surface` card with a Capsule/bar or numeral.

States: default (N events); empty (0 events) → centered empty state "Nothing to score yet" / "Your first analyzed exchange will show up here." with NO fabricated flat-line chart; single event → render a `PointMark`, not a line. VoiceOver: provide a text-equivalent summary of current score + trend (do not ship a silent chart). No view element displays or requests a match name.
  </action>
  <verify>
    <automated>grep -q "Scoped to this conversation only" BanterApp/Calculator/ConversationHealthView.swift && grep -q "import Charts" BanterApp/Calculator/ConversationHealthView.swift && echo "health-view-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `ConversationHealthView.swift` is parameterized by `conversationId: UUID` and reads only via `SentimentTimelineStore.events(for:)`.
    - Contains the exact caption "Scoped to this conversation only — never shared across matches." and `import Charts`.
    - Empty state renders "Nothing to score yet" with no chart; single-event renders a point, not a line.
    - No view/model method in this file accepts a match-name/identity parameter; no match name is displayed.
    - Provides a VoiceOver text-equivalent summary for the chart.
  </acceptance_criteria>
  <done>A per-conversation health score, sentiment timeline chart, and factor grid render from on-device events, with the CALC-03 scope caption always present and an accessible chart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coaching response → on-device timeline store | Aggregate sentiment persisted locally, per-conversation only |
| timeline store → health view | Read path must never cross conversation boundaries or key on identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-DOSSIER | Information Disclosure | SentimentTimelineStore | high | mitigate | All storage keyed strictly on conversationId (client-minted UUID, no PII); CALC-03 negative structural test forbids match-identity tokens; no cross-conversation aggregation exists |
| T-04-04-GROWTH | Denial of Service | SentimentTimelineStore | low | accept | Per-conversation events capped at 200 (Pitfall 1); conversations are naturally short — accepted risk for v1, no unbounded UserDefaults growth |
</threat_model>

<verification>
- `swift test --package-path BanterShared --filter SentimentTimelineStoreTests` green (round-trip + isolation + CALC-03 guard).
- CI XCUITest renders the health view (default/empty/single-point) from seeded static events.
- Grep guard: scope caption + `import Charts` present.
</verification>

<success_criteria>
A user views a per-conversation health score with an emotional-factor timeline, scoped to that conversation only (no match dossier), enforced by construction and a negative test.
</success_criteria>

<output>
Create `.planning/phases/04-companion-app-ui-paywall/04-04-SUMMARY.md` when done
</output>
