---
phase: 04-companion-app-ui-paywall
plan: 07
type: execute
wave: 2
depends_on: ["04-06"]
files_modified:
  - BanterApp/Coaching/CoachingResultModel.swift
  - BanterApp/Home/HomeModel.swift
  - BanterApp/Home/HomeView.swift
autonomous: true
gap_closure: true
requirements: [CALC-02, CALC-03]
tags: [ios, swiftui, charts, love-calculator, sentiment, wiring]

must_haves:
  truths:
    - "A production call site appends a SentimentEvent (built from CoachingResponseDTO.sentiment) to SentimentTimelineStore on each real coaching response — the store is written outside its own XCTest for the first time (CALC-02)."
    - "ConversationHealthView has >=1 production call site — reachable via navigation from the Home surface for the current conversationId (CALC-02)."
  artifacts:
    - "BanterApp/Coaching/CoachingResultModel.swift (adds an optional onResponse injection point; internals otherwise unchanged)"
    - "BanterApp/Home/HomeModel.swift (mints a conversationId, owns a SentimentTimelineStore, supplies onResponse that appends sentiment)"
    - "BanterApp/Home/HomeView.swift (navigation entry to ConversationHealthView for the current conversationId)"
  key_links:
    - "CoachingResultModel.selectTone calls onResponse?(response) within the newest-generation guard, right after replies = response.replies (mirrors onAnalysisRecorded?())"
    - "HomeModel supplies onResponse { sentimentStore.append(from: response, conversationId: conversationId, messageIndex:, speaker: .match) }"
    - "HomeView navigates to ConversationHealthView(conversationId: model.conversationId, store: model.sentimentStore)"
  prohibitions:
    - "SentimentTimelineStore and ConversationHealthView are keyed by conversationId (UUID) only — no match-name/identity parameter is introduced (CALC-03); SentimentTimelineStoreTests negative guard stays green"
    - "onResponse is additive and optional (default nil); the ValueDemoCoordinatorView onboarding demo passes nil, so the demo path still appends NO sentiment and its behavior is byte-identical (ONBD-01)"
    - "No tier branch is added around psychology-tag rendering (MONE-01); the health wiring does not touch SuggestionCardView"
    - "SentimentTimelineStore.swift and ConversationHealthView.swift internals are not modified — construct/append/navigate only"
---

<objective>
Close verification Gap 2 (Roadmap SC3 / CALC-02, CALC-03). ConversationHealthView and SentimentTimelineStore are well-built and unit-tested, but nothing in production ever writes a sentiment event or navigates to the view — it is unreachable dead code that would only ever render its empty state. This plan supplies the two missing links: (1) a real write path that appends a SentimentEvent from each coaching response, and (2) a navigation entry to ConversationHealthView. It builds on the Home surface created in 04-06 and does not rewrite either artifact's internals.

Purpose: Make the per-conversation love-calculator timeline reachable and populated in the shipping app, strictly conversationId-scoped (no match dossier).
Output: an additive onResponse injection on CoachingResultModel, the sentiment write + conversationId in HomeModel, and a ConversationHealthView navigation entry in HomeView.

NOTE (verification host): No local Swift toolchain on this Windows host. All `<verify>` checks are grep-based structural assertions runnable in Git Bash; compile/test proof is deferred to CI, consistent with the Phase 1-4 precedent.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-companion-app-ui-paywall/04-VERIFICATION.md
@.planning/phases/04-companion-app-ui-paywall/04-UI-SPEC.md

# Depends on the Home surface created in 04-06:
@.planning/phases/04-companion-app-ui-paywall/04-06-PLAN.md

# Artifacts being wired (do NOT modify their internals — inject/append/navigate only):
@BanterApp/Coaching/CoachingResultModel.swift
@BanterShared/Sources/BanterShared/Calculator/SentimentTimelineStore.swift
@BanterApp/Calculator/ConversationHealthView.swift
@BanterShared/Sources/BanterShared/NetworkDTOs.swift
@BanterShared/Sources/BanterShared/Models/SentimentEvent.swift
</context>

## Artifacts this phase produces (Plan 07)

New symbols introduced by this plan (exclude from drift verification):
- `CoachingResultModel.onResponse` — new optional `((CoachingResponseDTO) -> Void)?` initializer parameter (default nil), stored and called in `selectTone`.
- `HomeModel.conversationId` (client-minted `UUID`) + `HomeModel.sentimentStore` (`SentimentTimelineStore`) — added to the class created in 04-06.
- A ConversationHealthView navigation entry (NavigationLink / toolbar button) in `HomeView`.

<tasks>

<task type="auto">
  <name>Task 1: Add an optional onResponse injection point to CoachingResultModel</name>
  <files>BanterApp/Coaching/CoachingResultModel.swift</files>
  <read_first>
    - BanterApp/Coaching/CoachingResultModel.swift (existing init with capGate/onAnalysisRecorded optional closures; selectTone(_:) async, requestGeneration staleness guard, the `replies = response.replies` / `onAnalysisRecorded?()` sequence)
    - BanterShared/Sources/BanterShared/NetworkDTOs.swift (CoachingResponseDTO: replies, sentiment: SentimentDTO, conversationId: UUID?)
  </read_first>
  <action>
Add ONE additive optional closure to CoachingResultModel, mirroring the existing `onAnalysisRecorded` pattern exactly (this is the shape the file already uses for injected side effects — do not restructure the class):
- Add an initializer parameter `onResponse: ((CoachingResponseDTO) -> Void)? = nil`, stored as a `private let onResponse: ((CoachingResponseDTO) -> Void)?`. Place it after `onAnalysisRecorded` in the parameter list so all existing call sites (which use the trailing defaults) keep compiling unchanged.
- In `selectTone(_:)`, after the successful decode where `replies = response.replies` and `onAnalysisRecorded?()` are set, add `onResponse?(response)` — INSIDE the same `guard generation == requestGeneration else { return }` newest-generation block, so a stale/superseded request never appends a sentiment event. Pass the full decoded `response` (the `CoachingResponseDTO`).
- Change nothing else: capGate/onAnalysisRecorded semantics, the dailyCapReached flag, the generation counter, and the demo-path default-nil behavior all stay exactly as they are. Because the default is nil, the onboarding demo (ValueDemoCoordinatorView) — which does not pass onResponse — continues to append no sentiment, preserving the ONBD-01 demo behavior byte-for-byte.
  </action>
  <verify>
    <automated>grep -Eq 'onResponse: *\(\(CoachingResponseDTO\) *-> *Void\)\?' BanterApp/Coaching/CoachingResultModel.swift && echo onresponse-param</automated>
    <automated>grep -q 'onResponse?(response)' BanterApp/Coaching/CoachingResultModel.swift && echo onresponse-called</automated>
    <automated>grep -q 'guard generation == requestGeneration' BanterApp/Coaching/CoachingResultModel.swift && echo generation-guard-intact</automated>
  </verify>
  <acceptance_criteria>
    - CoachingResultModel gains an optional `onResponse: ((CoachingResponseDTO) -> Void)? = nil` initializer parameter (defaulted, trailing) and a stored property for it.
    - `onResponse?(response)` is invoked in `selectTone` within the newest-generation guard, after `replies = response.replies`.
    - No existing behavior changes: capGate/onAnalysisRecorded/dailyCapReached and the demo-path nil default are untouched; existing `CoachingResultModel(...)` call sites still compile without modification.
  </acceptance_criteria>
  <done>CoachingResultModel exposes an additive response hook that fires exactly once per non-stale coaching response, without altering any existing behavior or the ungated demo path.</done>
</task>

<task type="auto">
  <name>Task 2: Append sentiment + navigate to ConversationHealthView from the Home surface</name>
  <files>BanterApp/Home/HomeModel.swift, BanterApp/Home/HomeView.swift</files>
  <read_first>
    - BanterApp/Home/HomeModel.swift (from 04-06: owns entitlement/capTracker/importModel/coaching; startCoaching builds the gated CoachingResultModel)
    - BanterApp/Home/HomeView.swift (from 04-06: inside ContentView's NavigationStack; renders the coaching surface)
    - BanterShared/Sources/BanterShared/Calculator/SentimentTimelineStore.swift (init(); append(from: CoachingResponseDTO, conversationId: UUID, messageIndex: Int, speaker: Speaker); events(forConversationId:))
    - BanterApp/Calculator/ConversationHealthView.swift (init(conversationId: UUID, store: SentimentTimelineStore = ...))
    - BanterShared/Sources/BanterShared/Models/ConversationMessage.swift (Speaker enum: .user / .match)
  </read_first>
  <action>
Wire the sentiment write path (CALC-02) into HomeModel:
- Add `let sentimentStore = SentimentTimelineStore()` and `let conversationId = UUID()` (client-minted, the CALC-03-safe key — no match identity). One HomeModel instance == one conversation; a fresh conversation is a fresh HomeModel/conversationId.
- In `startCoaching()` (from 04-06), extend the `CoachingResultModel(...)` construction to also pass `onResponse: { [sentimentStore, conversationId, importModel] response in sentimentStore.append(from: response, conversationId: conversationId, messageIndex: max(0, importModel.transcript.count - 1), speaker: .match) }`. Use the convenience initializer `append(from:conversationId:messageIndex:speaker:)` already on the store — do not hand-build the SentimentEvent. `.match` is used because the aggregate sentiment reflects the exchange's match-facing engagement; messageIndex uses the transcript tail (the chart keys marks by array offset, so the exact index is not load-bearing).

Wire the navigation path (CALC-02) into HomeView:
- HomeView is already inside ContentView's `NavigationStack` (04-06 routing), so add a navigation entry to the coaching/suggestions surface — a toolbar button (e.g. label "Conversation Health", SF Symbol `heart.text.square` or similar) or a `NavigationLink` — that presents `ConversationHealthView(conversationId: model.conversationId, store: model.sentimentStore)`. It must pass the SAME store instance and conversationId the writes use, so the view renders this conversation's real events (not a fresh empty store).

Do not modify SentimentTimelineStore.swift or ConversationHealthView.swift internals, and add no match-name/identity anywhere — every access stays keyed by `conversationId: UUID` (CALC-03).
  </action>
  <verify>
    <automated>grep -q 'SentimentTimelineStore()' BanterApp/Home/HomeModel.swift && grep -Eq 'conversationId *= *UUID\(\)' BanterApp/Home/HomeModel.swift && echo store-and-cid</automated>
    <automated>grep -Eq 'sentimentStore\.append\(from:' BanterApp/Home/HomeModel.swift && echo append-callsite</automated>
    <automated>grep -rEl 'SentimentTimelineStore' --include=*.swift BanterApp/Home | grep -q HomeModel && grep -rl '\.append(' --include=*.swift BanterApp/Home | grep -q HomeModel && echo store-written-in-production</automated>
    <automated>grep -Eq 'ConversationHealthView\(conversationId:' BanterApp/Home/HomeView.swift && echo health-nav-callsite</automated>
    <automated>grep -rl 'ConversationHealthView(' --include=*.swift BanterApp | grep -v 'Calculator/ConversationHealthView.swift' | grep -q Home && echo health-callsite-outside-own-decl</automated>
  </verify>
  <acceptance_criteria>
    - `SentimentTimelineStore` is constructed AND `.append(from:` is called in a production file (BanterApp/Home/HomeModel.swift) — the store is written outside SentimentTimelineStoreTests for the first time.
    - HomeModel mints a `conversationId = UUID()` and passes it (plus `speaker: .match`, a `messageIndex` Int) to the append — no match-name/identity string anywhere (CALC-03).
    - `ConversationHealthView(conversationId:` matches a production file (BanterApp/Home/HomeView.swift) outside its own declaration file, wired to the same `model.conversationId` + `model.sentimentStore` used for writes.
    - SentimentTimelineStore.swift and ConversationHealthView.swift internals are unmodified; SuggestionCardView is untouched (no tag-tier branch — MONE-01 holds).
  </acceptance_criteria>
  <done>Each real coaching response appends a conversationId-scoped sentiment event, and the user can navigate from Home to a populated ConversationHealthView for that conversation — closing SC3 while preserving the CALC-03 no-dossier boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coaching response → on-device sentiment store | Aggregate sentiment from CoachingResponseDTO is persisted per-conversation on-device |
| conversationId scoping → health view read | The view reads only events for one client-minted conversationId (CALC-03 no-dossier boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07-DOSSIER | Information Disclosure (spec) | SentimentTimelineStore + ConversationHealthView wiring | high | mitigate | Append and view are keyed by `conversationId: UUID` only; no match-name/identity is passed. The CALC-03 no-dossier boundary is preserved by construction and tripwired by SentimentTimelineStoreTests' forbidden-token guard (store internals unchanged). |
| T-04-07-GROWTH | Denial of Service | SentimentTimelineStore | low | accept | The store already caps at 200 events/conversation; this wiring appends one event per analysis. Unbounded growth is an inherited accepted risk (T-04-04-GROWTH), unchanged. |

No package-manager installs occur in this plan; no supply-chain checkpoint is required.
</threat_model>

<verification>
- Grep guards (Git Bash, this host): CoachingResultModel exposes onResponse and calls it under the generation guard; HomeModel constructs SentimentTimelineStore, mints conversationId, and calls `.append(from:`; ConversationHealthView( has a production call site in HomeView outside its own file.
- Invariant guards stay green: append/view keyed by conversationId (UUID) only, no match-identity param (CALC-03); onResponse default nil keeps the demo path append-free (ONBD-01); SuggestionCardView unchanged (MONE-01); SentimentTimelineStore/ConversationHealthView internals unchanged.
- Compile/UI-test proof deferred to CI (no local Swift toolchain), consistent with Phases 1-4.
</verification>

<success_criteria>
Each real coaching response writes a conversationId-scoped SentimentEvent to SentimentTimelineStore, and ConversationHealthView is reachable from the Home surface for that conversation, rendering the populated score/timeline/factor grid — closing Roadmap SC3 (CALC-02) while the CALC-03 no-dossier boundary provably holds.
</success_criteria>

<output>
Create `.planning/phases/04-companion-app-ui-paywall/04-07-SUMMARY.md` when done.
</output>
