---
phase: 05-keyboard-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BanterShared/Sources/BanterShared/CachedSuggestionsStorageKey.swift
  - BanterApp/Home/HomeModel.swift
  - BanterShared/Tests/BanterSharedTests/CachedSuggestionsRoundTripTests.swift
  - BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift
autonomous: true
requirements: [KEYS-01, KEYS-03]
must_haves:
  truths:
    - "The app persists the latest [ReplySuggestion] into the App Group on every coaching response (HomeModel.startCoaching onResponse writes CachedSuggestionsStorageKey.suggestions)"
    - "A round-trip test proves [ReplySuggestion] written under CachedSuggestionsStorageKey.suggestions reads back equal"
    - "BanterKeyboard/ target sources contain zero network/RevenueCat tokens, enforced by a structural guard test (KEYS-03)"
  artifacts:
    - "BanterShared/Sources/BanterShared/CachedSuggestionsStorageKey.swift"
    - "BanterShared/Tests/BanterSharedTests/CachedSuggestionsRoundTripTests.swift"
    - "BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift"
  key_links:
    - "HomeModel.startCoaching's onResponse closure calls AppGroupStore.write(response.replies, forKey: CachedSuggestionsStorageKey.suggestions) — the sole producer of the keyboard's cache"
---

<objective>
Deliver the app-side write half of the keyboard slice: a public typed storage key both targets can reference, a production call site in HomeModel that caches the latest suggestions into the App Group on every coaching response, and the two Wave-0 guard tests (round-trip + KEYS-03 network boundary) that lock in KEYS-01 and KEYS-03.

Purpose: The keyboard (Plan 02) is a read-only consumer. Nothing exists for it to read until the app writes. This plan closes the Phase 4 "built but unwired" lesson by making the write a real production call site with a must_have truth, not an assumption.
Output: CachedSuggestionsStorageKey.swift, an additive one-line change to HomeModel.startCoaching, and two new BanterShared tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-keyboard-extension/05-RESEARCH.md
@.planning/phases/05-keyboard-extension/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (05-01) produces:
- **New file** `BanterShared/Sources/BanterShared/CachedSuggestionsStorageKey.swift`
  - `public enum CachedSuggestionsStorageKey` with `public static let suggestions = "cached_suggestions"`
- **New file** `BanterShared/Tests/BanterSharedTests/CachedSuggestionsRoundTripTests.swift`
  - `final class CachedSuggestionsRoundTripTests: XCTestCase` with `func testCachedSuggestionsRoundTrip()`
- **New file** `BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift`
  - `final class KeyboardNetworkBoundaryGuardTests: XCTestCase` with `func testBanterKeyboardSourcesContainNoNetworkOrRevenueCatTokens()`
- **Modified** `BanterApp/Home/HomeModel.swift` — `startCoaching()`'s existing `onResponse` closure gains one `AppGroupStore.write(...)` line (no new capture; `AppGroupStore`/`CachedSuggestionsStorageKey` are static)

Storage key string introduced: `"cached_suggestions"` (single overwrite-on-every-response key, mirrors HomeModel's `lastKnownPremiumKey` single-key pattern).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public CachedSuggestionsStorageKey + failing round-trip test</name>
  <files>BanterShared/Sources/BanterShared/CachedSuggestionsStorageKey.swift, BanterShared/Tests/BanterSharedTests/CachedSuggestionsRoundTripTests.swift</files>
  <read_first>
    - BanterApp/Paywall/DowngradeBanner.swift (lines 41-46 — DowngradeBannerStorageKey analog; note it is internal, ours must be public)
    - BanterShared/Tests/BanterSharedTests/AppGroupRoundTripTests.swift (exact round-trip structure to mirror, esp. testReplySuggestionRoundTrips)
    - BanterShared/Sources/BanterShared/Models/ReplySuggestion.swift (Equatable Codable struct: fields text/psychologyTag/style; ReplyStyle enum)
    - BanterShared/Sources/BanterShared/AppGroupStore.swift (write<T:Codable>/read<T:Codable> generic signatures)
    - .planning/phases/05-keyboard-extension/05-PATTERNS.md (Typed storage key section, lines 164-178)
  </read_first>
  <behavior>
    - Test writes `[ReplySuggestion(text: "hey!", psychologyTag: "reciprocity", style: .playful)]` via AppGroupStore.write under CachedSuggestionsStorageKey.suggestions, reads back `[ReplySuggestion].self`, asserts equal.
    - The key is public so BanterKeyboard (a separate SPM consumer) can reference it in Plan 02.
  </behavior>
  <action>
    Create `BanterShared/Sources/BanterShared/CachedSuggestionsStorageKey.swift` declaring `public enum CachedSuggestionsStorageKey { public static let suggestions = "cached_suggestions" }`. It MUST be `public` (both `public` on the enum and on the static let) — unlike the internal `DowngradeBannerStorageKey`, because BanterKeyboard reads this key across the package boundary. No other members.

    Create `BanterShared/Tests/BanterSharedTests/CachedSuggestionsRoundTripTests.swift`: `import XCTest` + `@testable import BanterShared`, `final class CachedSuggestionsRoundTripTests: XCTestCase`, one method `func testCachedSuggestionsRoundTrip()` mirroring AppGroupRoundTripTests.testReplySuggestionRoundTrips exactly but on an ARRAY: build `[ReplySuggestion]`, write under `CachedSuggestionsStorageKey.suggestions`, read `[ReplySuggestion].self`, `XCTAssertEqual`. Use the real key constant (not a literal string) — this test IS the contract proof for that key.

    Do not modify HomeModel in this task (Task 2). Do not shell out to grep from tests — use the codebase's String(contentsOf:)/.contains structural style.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter CachedSuggestionsRoundTripTests</automated>
    (CI-run; on this Windows host executor records the expectation and confirms via CI — no local Swift toolchain, per Phase 1-4 precedent.)
  </verify>
  <acceptance_criteria>
    - `grep -q 'public enum CachedSuggestionsStorageKey' BanterShared/Sources/BanterShared/CachedSuggestionsStorageKey.swift`
    - `grep -q 'public static let suggestions = "cached_suggestions"' BanterShared/Sources/BanterShared/CachedSuggestionsStorageKey.swift`
    - `grep -q 'CachedSuggestionsStorageKey.suggestions' BanterShared/Tests/BanterSharedTests/CachedSuggestionsRoundTripTests.swift`
    - `grep -q '\[ReplySuggestion\].self' BanterShared/Tests/BanterSharedTests/CachedSuggestionsRoundTripTests.swift`
    - CI: `swift test --package-path BanterShared --filter CachedSuggestionsRoundTripTests` exits 0
  </acceptance_criteria>
  <done>The public storage key exists and a round-trip test proves an array of ReplySuggestion survives an AppGroupStore write/read under that exact key.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the write call into HomeModel.startCoaching (KEYS-01 producer)</name>
  <files>BanterApp/Home/HomeModel.swift</files>
  <read_first>
    - BanterApp/Home/HomeModel.swift (startCoaching, lines 60-71 — the existing onResponse closure with its [sentimentStore, conversationId, importModel] capture)
    - .planning/phases/05-keyboard-extension/05-PATTERNS.md (HomeModel MODIFY section, lines 182-209 — the exact additive diff)
    - .planning/phases/05-keyboard-extension/05-RESEARCH.md (Code Examples: "The one new write call", lines 274-294)
  </read_first>
  <action>
    In `BanterApp/Home/HomeModel.swift`, inside `startCoaching()`'s existing `onResponse` closure, add ONE line after the existing `sentimentStore.append(...)` call: `AppGroupStore.write(response.replies, forKey: CachedSuggestionsStorageKey.suggestions)`. Keep the existing capture list `[sentimentStore, conversationId, importModel]` unchanged — `AppGroupStore` and `CachedSuggestionsStorageKey` are static/enum, so no new captured value is needed. Precede the line with a short comment noting it caches the latest suggestions for the keyboard (KEYS-01). This is a single overwrite-on-every-response write, matching the `lastKnownPremiumKey`/`DowngradeBannerStorageKey` single-key idiom already in this file. Do not restructure the closure or the method.

    `BanterShared` is already imported at the top of HomeModel.swift — no new import needed.
  </action>
  <verify>
    <automated>grep -q 'AppGroupStore.write(response.replies, forKey: CachedSuggestionsStorageKey.suggestions)' BanterApp/Home/HomeModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AppGroupStore.write(response.replies, forKey: CachedSuggestionsStorageKey.suggestions)' BanterApp/Home/HomeModel.swift` returns 1
    - `grep -c 'onResponse:' BanterApp/Home/HomeModel.swift` still returns 1 (closure not duplicated/restructured)
    - The write appears inside the `onResponse` closure body (after `sentimentStore.append`), not in a new method
    - CI: BanterApp scheme builds (write call compiles against the public key)
  </acceptance_criteria>
  <done>HomeModel.startCoaching persists response.replies into the App Group under CachedSuggestionsStorageKey.suggestions on every coaching response — a real production call site, not an assumption.</done>
</task>

<task type="auto">
  <name>Task 3: KEYS-03 structural network-boundary guard scanning BanterKeyboard/</name>
  <files>BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift</files>
  <read_first>
    - BanterShared/Tests/BanterSharedTests/NetworkBoundaryGuardTests.swift (whole file — #filePath navigation + forbidden-token loop to copy)
    - .planning/phases/05-keyboard-extension/05-PATTERNS.md (KeyboardNetworkBoundaryGuardTests section, lines 305-335 — the directory-enumeration variant)
    - .planning/phases/05-keyboard-extension/05-RESEARCH.md (Validation Architecture KEYS-03 row + Wave 0 Gaps)
  </read_first>
  <action>
    Create `BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift` mirroring NetworkBoundaryGuardTests' structural style: `final class KeyboardNetworkBoundaryGuardTests: XCTestCase`, one throwing method `func testBanterKeyboardSourcesContainNoNetworkOrRevenueCatTokens()`. Navigate from `#filePath` up FOUR `.deletingLastPathComponent()` levels (BanterSharedTests/ -> Tests/ -> BanterShared/ package root -> repo root) then `.appendingPathComponent("BanterKeyboard")`. Enumerate that directory with `FileManager.default.enumerator(at:includingPropertiesForKeys:)`, and for each `.swift` file read `String(contentsOf:encoding:.utf8)` and `XCTAssertFalse(source.contains(token), ...)` for each forbidden token.

    Forbidden token list: name the URLSession identifier, the RevenueCat import statement, and the Network framework import statement as the three checked tokens (this matches the research's `URLSession|import RevenueCat|import Network` scan). Do NOT place any of these forbidden literals anywhere else in a written keyboard source file, and do not add them as comment text — the guard scans raw source.

    Read source via String(contentsOf:), never a grep subprocess — matches this codebase's established structural-guard style (NetworkBoundaryGuardTests / GeminiKeyBoundaryGuardTests). Assertion failure message must name the offending token and file and cite the KEYS-03 boundary.
  </action>
  <verify>
    <automated>swift test --package-path BanterShared --filter KeyboardNetworkBoundaryGuardTests</automated>
    (CI-run. Also mirrorable locally as: `grep -riE 'URLSession|import RevenueCat|import Network' BanterKeyboard/` must find nothing — but the durable gate is the XCTest.)
  </verify>
  <acceptance_criteria>
    - `grep -q 'final class KeyboardNetworkBoundaryGuardTests' BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift`
    - `grep -q 'FileManager.default.enumerator' BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift`
    - `grep -q 'BanterKeyboard' BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift`
    - `grep -q 'XCTAssertFalse' BanterShared/Tests/BanterSharedTests/KeyboardNetworkBoundaryGuardTests.swift`
    - Local mirror `grep -riE 'URLSession|import RevenueCat|import Network' BanterKeyboard/` returns no matches (Phase 1 placeholder is network-free)
    - CI: guard test passes green against the current (and Plan-02-rewritten) BanterKeyboard sources
  </acceptance_criteria>
  <done>A green structural guard fails CI the instant any network-capable or RevenueCat token appears anywhere under BanterKeyboard/ — KEYS-03 is enforced, not merely intended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BanterApp process → App Group container | The app writes user-intimate suggestion text into shared UserDefaults; the keyboard (a separate process) reads it |
| BanterKeyboard target source → any network/SDK | KEYS-03 structural boundary: the keyboard target must have no code path that can transmit off-device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | A future PR adds URLSession/RevenueCat/Network import to BanterKeyboard/ | high | mitigate | KeyboardNetworkBoundaryGuardTests (Task 3) fails CI on any such token; KEYS-03 enforced structurally |
| T-05-02 | Information Disclosure | Suggestion text (intimate) cached in App Group UserDefaults, overwritten but not encrypted-by-us | low | accept | Relies on iOS default data-protection for the App Group container, unchanged from Phase 1 posture; single-key overwrite keeps only the latest exchange, no history dossier accumulates |
| T-05-03 | Tampering | Cross-process concurrent write to the cache key | low | accept | Only BanterApp writes this key (keyboard is read-only per research); no cross-process writer race exists by design |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan adds zero package-manager installs (no new SPM/npm/pip/cargo dependency); no [ASSUMED]/[SUS] packages to gate |
</threat_model>

<verification>
CI-verifiable (automated, this plan's gate):
- `swift test --package-path BanterShared` green, including CachedSuggestionsRoundTripTests and KeyboardNetworkBoundaryGuardTests.
- BanterApp scheme builds with the new write call.
- `grep -riE 'URLSession|import RevenueCat|import Network' BanterKeyboard/` finds nothing.

No human/device verification required for this plan — it is all structural/unit scope.
</verification>

<success_criteria>
- Public CachedSuggestionsStorageKey exists and is referenced by both the round-trip test and (in Plan 02) the keyboard.
- HomeModel.startCoaching writes response.replies to the App Group on every response (KEYS-01 producer).
- KEYS-03 guard test is green and scans the whole BanterKeyboard/ directory.
</success_criteria>

<output>
Create `.planning/phases/05-keyboard-extension/05-01-SUMMARY.md` when done.
</output>
