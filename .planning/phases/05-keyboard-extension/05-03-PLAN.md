---
phase: 05-keyboard-extension
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - BanterApp/Onboarding/PermissionPrimingView.swift
  - BanterApp/Home/KeyboardEnableBanner.swift
  - BanterApp/Home/HomeView.swift
  - BanterApp/Info.plist
autonomous: true
requirements: [KEYS-04]
must_haves:
  truths:
    - "A guided keyboard-enable flow exists via PermissionPrimingView.keyboard(...) with numbered Settings steps and an explicit 'Full Access not needed' reassurance line (KEYS-04)"
    - "The flow's primary CTA deep-links to Keyboard settings via prefs:root=General&path=Keyboard (enabled by a prefs URL type in BanterApp Info.plist)"
    - "HomeView renders a KeyboardEnableBanner that routes to the guided flow (production call site — KEYS-04 is reachable, not built-and-unwired)"
    - "The banner and post-enable state fail open: best-effort AppleKeyboards detection only changes copy/CTA presence, never hides functionality"
  artifacts:
    - "BanterApp/Onboarding/PermissionPrimingView.swift (additive steps + reassurance params + .keyboard(...) factory)"
    - "BanterApp/Home/KeyboardEnableBanner.swift"
    - "BanterApp/Info.plist (prefs URL type)"
  key_links:
    - "HomeView -> KeyboardEnableBanner -> presents PermissionPrimingView.keyboard(...) -> prefs: deep link (the KEYS-04 chain, all wired)"
    - "PermissionPrimingView.keyboard(...) reachable from a BanterApp production call site, grep-verifiable"
---

<objective>
Deliver the guided keyboard-enable slice (KEYS-04): extend the already-generic PermissionPrimingView additively with a numbered-steps parameter, a reassurance line, and a .keyboard(...) factory; add a prefs URL type to BanterApp's Info.plist so the CTA can deep-link straight to Keyboard settings; and surface it from HomeView via a KeyboardEnableBanner (same visual family as DowngradeBanner) so the flow has a real production call site.

Purpose: Turns "we built an enable screen" into "a user on Home is nudged to enable the keyboard and walked through it" — the Phase 4 lesson that every new surface needs a production call site in the same phase. Detection is best-effort and fails open.
Output: additive edits to PermissionPrimingView.swift, a new KeyboardEnableBanner.swift, a HomeView integration, and the Info.plist prefs URL type.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-keyboard-extension/05-RESEARCH.md
@.planning/phases/05-keyboard-extension/05-PATTERNS.md
@.planning/phases/05-keyboard-extension/05-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan (05-03) produces:
- **Modified** `BanterApp/Onboarding/PermissionPrimingView.swift`
  - Additive init params `steps: [String]? = nil` and `reassurance: String? = nil` (both nil-defaulted → existing `.photos(...)` call site unchanged)
  - Rendered `steps` Text block + `reassurance` Text between body and primary CTA
  - New `static func keyboard(onContinue:onSkip:) -> PermissionPrimingView`
- **New file** `BanterApp/Home/KeyboardEnableBanner.swift`
  - `struct KeyboardEnableBanner: View` (DowngradeBanner visual family) + a best-effort `isKeyboardLikelyEnabled(bundleID:) -> Bool` helper reading `AppleKeyboards`
  - `enum KeyboardEnableBannerStorageKey { static let dismissed = "keyboardEnableBanner.dismissed" }`
- **Modified** `BanterApp/Home/HomeView.swift` — renders KeyboardEnableBanner in the top VStack slot (mirrors the DowngradeBanner conditional), presents PermissionPrimingView.keyboard(...) on tap
- **Modified** `BanterApp/Info.plist` — `CFBundleURLTypes` array with scheme `prefs`

Deep-link string introduced: `prefs:root=General&path=Keyboard` (QA1924, callable from the containing app).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend PermissionPrimingView additively + .keyboard(...) factory</name>
  <files>BanterApp/Onboarding/PermissionPrimingView.swift</files>
  <read_first>
    - BanterApp/Onboarding/PermissionPrimingView.swift (whole file — the base struct, init, body layout, and existing .photos(...) factory to mirror)
    - .planning/phases/05-keyboard-extension/05-PATTERNS.md (PermissionPrimingView MODIFY section, lines 213-260 — the two additive changes + factory body)
    - .planning/phases/05-keyboard-extension/05-UI-SPEC.md (Screen 5.2 layout points 4/5/6/7 + Copywriting Contract exact strings)
    - BanterApp/Onboarding/ValueDemoCoordinatorView.swift (existing .photos(...) call site — must stay unchanged since new params are nil-defaulted)
  </read_first>
  <action>
    Modify `BanterApp/Onboarding/PermissionPrimingView.swift`:

    1. Add two additive, nil-defaulted stored properties + init params: `steps: [String]? = nil` and `reassurance: String? = nil`. Update the memberwise `init` to accept them with `nil` defaults so the existing `.photos(...)` factory and the ValueDemoCoordinatorView call site compile unchanged (zero-change backward compatibility).

    2. In `body`, between the body-copy `Text` and the primary Continue button, insert: `if let steps { Text(steps.joined(separator: "\n")) }` styled `Banter.TextStyle.body` / `Banter.Colors.textPrimary`, left-aligned (`.multilineTextAlignment(.leading)`, `.frame(maxWidth: .infinity, alignment: .leading)`), horizontal `Banter.Spacing.md` padding — a single Text block, not numbered-list UI chrome (ladder-rung simplicity). Then `if let reassurance { Text(reassurance) }` styled `Banter.TextStyle.label` / `Banter.Colors.textSecondary`, centered.

    3. Add the new factory:
       `static func keyboard(onContinue: @escaping () -> Void, onSkip: @escaping () -> Void) -> PermissionPrimingView` with `icon: "keyboard"`, heading `Type without switching apps`, body `Turn on the Banter keyboard to insert your suggestions directly into any chat — no copy-paste, no app-switching.`, and the three step strings EXACTLY:
       - `1. Open Settings → General → Keyboard → Keyboards`
       - `2. Tap Add New Keyboard, then choose Banter`
       - `3. Tap Banter in the list and make sure Allow Full Access stays off — Banter never needs it`
       and `reassurance:` = `Banter's keyboard never needs Full Access and never connects to the internet on its own.`

    The primary Continue button already exists in the base view; the .keyboard(...) factory's onContinue is where the caller will fire the prefs deep link (Task 3) — do NOT hardcode the deep link inside PermissionPrimingView; keep the component generic (the button label stays "Continue" from the base view; the caller supplies the action).

    Keep the existing dark-canonical palette for this ordinary BanterApp screen — no host-appearance logic here (that is keyboard-surface-only).
  </action>
  <verify>
    <automated>grep -q 'static func keyboard(onContinue' BanterApp/Onboarding/PermissionPrimingView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'steps: \[String\]? = nil' BanterApp/Onboarding/PermissionPrimingView.swift`
    - `grep -q 'reassurance: String? = nil' BanterApp/Onboarding/PermissionPrimingView.swift`
    - `grep -q 'static func keyboard(onContinue' BanterApp/Onboarding/PermissionPrimingView.swift`
    - `grep -q 'Type without switching apps' BanterApp/Onboarding/PermissionPrimingView.swift`
    - `grep -q 'never needs Full Access' BanterApp/Onboarding/PermissionPrimingView.swift`
    - existing `.photos(...)` factory and its ValueDemoCoordinatorView call site are byte-unchanged in signature (new params nil-defaulted): `grep -q 'static func photos(onContinue' BanterApp/Onboarding/PermissionPrimingView.swift`
    - CI: BanterApp scheme builds; existing PermissionPrimingTests stay green
  </acceptance_criteria>
  <done>PermissionPrimingView gains a nil-defaulted steps + reassurance param and a .keyboard(...) factory, with the Photos call site unchanged — the guided-enable content exists, reusably.</done>
</task>

<task type="auto">
  <name>Task 2: KeyboardEnableBanner + best-effort fail-open detection</name>
  <files>BanterApp/Home/KeyboardEnableBanner.swift</files>
  <read_first>
    - BanterApp/Paywall/DowngradeBanner.swift (whole file — exact HStack/VStack banner recipe + co-located StorageKey enum to mirror)
    - .planning/phases/05-keyboard-extension/05-PATTERNS.md (HomeView MODIFY section, lines 264-282; storage-key/banner pattern)
    - .planning/phases/05-keyboard-extension/05-UI-SPEC.md (Screen 5.3 layout/states + Copywriting Contract badge/CTA strings; fail-open rule)
    - .planning/phases/05-keyboard-extension/05-RESEARCH.md (Pattern 3 detection is best-effort, lines 186-201; Assumption A1 fail-open)
  </read_first>
  <action>
    Create `BanterApp/Home/KeyboardEnableBanner.swift`: `import SwiftUI` + `import BanterShared`. Declare `struct KeyboardEnableBanner: View` with a `let onTap: () -> Void`, using DowngradeBanner's exact container recipe verbatim: `HStack(alignment: .top, spacing: Banter.Spacing.sm)` with `Image(systemName: "keyboard").foregroundStyle(Banter.Colors.textSecondary).accessibilityHidden(true)` + a `VStack(alignment: .leading, spacing: Banter.Spacing.xs)` containing heading `Text("Enable the Banter keyboard")` (`Banter.TextStyle.label`, bold weight via existing discipline, `textPrimary`) and supporting `Text("Insert suggestions in any chat app")` (`Banter.TextStyle.label`, `textSecondary`); container `.padding(Banter.Spacing.md).frame(maxWidth: .infinity, alignment: .leading).background(Banter.Colors.surface).clipShape(RoundedRectangle(cornerRadius: Banter.Radius.lg))`. Make the whole banner tappable (`.contentShape(Rectangle()).onTapGesture { onTap() }` or wrap in a Button) with a 44pt+ min height.

    Add a co-located `enum KeyboardEnableBannerStorageKey { static let dismissed = "keyboardEnableBanner.dismissed" }` mirroring DowngradeBannerStorageKey.

    Add a best-effort detection helper `func isKeyboardLikelyEnabled(bundleID: String) -> Bool` reading `UserDefaults.standard.array(forKey: "AppleKeyboards") as? [String]` and returning `.contains(bundleID)`, else `false`. Mark it clearly as best-effort/undocumented (05-RESEARCH.md Assumption A1). This is used ONLY to change banner visibility/copy — it MUST fail open: on nil/uncertain read, default to SHOWING the banner (a harmless nudge), never to hiding a KEYS-04 affordance.

    Add a SwiftUI `#Preview` for the banner (static, CI-renderable per 05-UI-SPEC.md).
  </action>
  <verify>
    <automated>grep -q 'struct KeyboardEnableBanner' BanterApp/Home/KeyboardEnableBanner.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'struct KeyboardEnableBanner: View' BanterApp/Home/KeyboardEnableBanner.swift`
    - `grep -q 'Enable the Banter keyboard' BanterApp/Home/KeyboardEnableBanner.swift`
    - `grep -q 'Insert suggestions in any chat app' BanterApp/Home/KeyboardEnableBanner.swift`
    - `grep -q 'AppleKeyboards' BanterApp/Home/KeyboardEnableBanner.swift`
    - `grep -q 'KeyboardEnableBannerStorageKey' BanterApp/Home/KeyboardEnableBanner.swift`
    - `grep -q '#Preview' BanterApp/Home/KeyboardEnableBanner.swift`
    - CI: BanterApp scheme builds
  </acceptance_criteria>
  <done>A DowngradeBanner-family KeyboardEnableBanner exists with fail-open best-effort detection and a dismissal storage key — the reusable Home nudge component.</done>
</task>

<task type="auto">
  <name>Task 3: Wire banner into HomeView + present guided flow + prefs URL type + deep link</name>
  <files>BanterApp/Home/HomeView.swift, BanterApp/Info.plist</files>
  <read_first>
    - BanterApp/Home/HomeView.swift (whole file — the top-of-VStack DowngradeBanner conditional slot to mirror; the .sheet/showPaywall presentation idiom)
    - BanterApp/Info.plist (current 8-line plist — add CFBundleURLTypes)
    - .planning/phases/05-keyboard-extension/05-PATTERNS.md (prefs deep link primary CTA lines 254-261; Info.plist section lines 361-363)
    - .planning/phases/05-keyboard-extension/05-RESEARCH.md (Pitfall 5 — prefs:root=General&path=Keyboard IS callable from the containing app per QA1924)
    - .planning/phases/05-keyboard-extension/05-UI-SPEC.md (Screen 5.2 CTA point 6; Screen 5.3 States dismissal/fail-open)
  </read_first>
  <action>
    Modify `BanterApp/Info.plist`: add a `CFBundleURLTypes` array containing one dict with `CFBundleURLSchemes = [prefs]` (QA1924 setup step so `prefs:root=General&path=Keyboard` is openable from BanterApp). Plain plist XML, no Swift.

    Modify `BanterApp/Home/HomeView.swift`:
    - Add `@State private var showKeyboardEnable = false`.
    - At the top of the outer `VStack(spacing: 0)`, add a conditional slot mirroring the existing DowngradeBanner one: render `KeyboardEnableBanner(onTap: { showKeyboardEnable = true })` when the banner should show. Gate visibility on: NOT dismissed (`AppGroupStore.read(Bool.self, forKey: KeyboardEnableBannerStorageKey.dismissed) != true`) AND fail-open detection (show unless `isKeyboardLikelyEnabled(bundleID:)` is confidently true). Use the app extension's bundle id for the keyboard (derive/hardcode the keyboard extension bundle identifier consistent with project.yml; if uncertain, fail open and show the banner). Apply the same `.padding(.horizontal, Banter.Spacing.md).padding(.top, Banter.Spacing.sm)` as DowngradeBanner.
    - Present the guided flow: add a `.sheet(isPresented: $showKeyboardEnable)` (or a state-driven full-screen swap consistent with this file's convention) rendering `PermissionPrimingView.keyboard(onContinue: { openKeyboardSettings() }, onSkip: { dismissKeyboardBanner() })`.
    - Add a private `openKeyboardSettings()` that does `if let url = URL(string: "prefs:root=General&path=Keyboard") { UIApplication.shared.open(url) }`.
    - Add a private `dismissKeyboardBanner()` that writes `AppGroupStore.write(true, forKey: KeyboardEnableBannerStorageKey.dismissed)` and sets `showKeyboardEnable = false` — same one-shot dismissal shape as DowngradeBanner (05-UI-SPEC.md Screen 5.3 States: does not reappear after dismiss).

    Do not touch the existing DowngradeBanner slot, the coaching/import dispatcher, or the paywall sheet — additive only.
  </action>
  <verify>
    <automated>grep -q 'PermissionPrimingView.keyboard' BanterApp/Home/HomeView.swift</automated>
  </verify>
  <acceptance_criteria>
    - KEYS-04 production call site: `grep -q 'PermissionPrimingView.keyboard' BanterApp/Home/HomeView.swift` (outside PermissionPrimingView.swift itself)
    - `grep -q 'KeyboardEnableBanner(' BanterApp/Home/HomeView.swift`
    - `grep -q 'prefs:root=General&path=Keyboard' BanterApp/Home/HomeView.swift`
    - `grep -q 'KeyboardEnableBannerStorageKey.dismissed' BanterApp/Home/HomeView.swift`
    - Info.plist has the URL type: `grep -q 'CFBundleURLTypes' BanterApp/Info.plist` AND `grep -q '<string>prefs</string>' BanterApp/Info.plist`
    - existing DowngradeBanner conditional still present: `grep -q 'DowngradeBanner(' BanterApp/Home/HomeView.swift`
    - CI: BanterApp scheme builds; XCUITest screenshot suite green (banner-shown state renderable)
  </acceptance_criteria>
  <done>HomeView renders the KeyboardEnableBanner (fail-open), tapping it opens the guided flow, its CTA deep-links to Keyboard settings via the now-registered prefs scheme, and dismissal persists — KEYS-04 is fully wired to a production entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BanterApp → iOS Settings (prefs: deep link) | The app opens a system URL scheme; only the exact Keyboard-settings string is permitted (QA1924 / App Review) |
| Undocumented AppleKeyboards UserDefaults read | Best-effort detection of a private-adjacent key; result must never gate functionality |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Denial of Service (UX) | AppleKeyboards key format changes in a future iOS release, detection always returns "not enabled" | low | accept | Fail-open design: the banner/guided flow always renders its default state on uncertain reads; detection only changes copy/CTA presence, never hides KEYS-04 functionality (05-RESEARCH.md Assumption A1) |
| T-05-08 | Elevation of Privilege (App Review) | prefs: scheme used for anything other than the Keyboard-settings string, risking rejection | medium | mitigate | The exact string `prefs:root=General&path=Keyboard` is the only prefs: URL used; grep-asserted; used solely for the guided-enable CTA per QA1924 |
| T-05-09 | Spoofing | Copy implying the keyboard needs Full Access, nudging users to over-grant a privacy-sensitive permission | medium | mitigate | Reassurance line explicitly states Full Access is not needed (KEYS-03 user-facing expression); step 3 says keep it off — a hard copy requirement, not marketing |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — zero package installs this plan; no new SPM/npm/pip/cargo dependency |
</threat_model>

<verification>
CI-verifiable (automated, this plan's gate):
- BanterApp scheme builds with the additive PermissionPrimingView params, KeyboardEnableBanner, and HomeView wiring.
- Existing PermissionPrimingTests + XCUITest screenshot suite green (Photos call site unchanged; banner-shown state renderable as a #Preview / screenshot).
- grep asserts: `PermissionPrimingView.keyboard` reachable from HomeView; `prefs:root=General&path=Keyboard` present; `CFBundleURLTypes`/`prefs` in Info.plist.

Manual/device-only (05-VALIDATION.md Manual-Only table):
- Tap the enable CTA and confirm Settings opens at General → Keyboard (deep-link navigation is system UI).
- Confirm the guided step copy is legible and the "Full Access not needed" reassurance reads correctly.
Documented as human-verification items at the phase gate.
</verification>

<success_criteria>
- Guided enable flow exists (.keyboard factory with steps + reassurance) and is reachable from HomeView (KEYS-04, wired).
- Primary CTA deep-links to Keyboard settings via the registered prefs scheme.
- Detection is best-effort and fails open; dismissal persists.
</success_criteria>

<output>
Create `.planning/phases/05-keyboard-extension/05-03-SUMMARY.md` when done. Record the deep-link/Settings-navigation and copy-legibility manual checks as outstanding human items for the phase gate.
</output>
